export type ChapterFoundationBridge = {
  label: string;
  problem: string;
  normalPath: string;
  terms: string[];
};

type Rule = {
  volume: string;
  match: RegExp;
  bridge: ChapterFoundationBridge;
};

const rules: Rule[] = [
  {volume: 'volume-01', match: /virtual memory|page cache|swap|oom/i, bridge: {label: 'Memory foundation', problem: 'Programs need memory, while Linux must share finite RAM safely and keep the machine usable under pressure.', normalPath: 'process allocation → virtual address → physical page or cached file page → reclaim/limit decision → possible OOM action', terms: ['virtual address', 'resident memory', 'page', 'page cache', 'swap', 'reclaim', 'cgroup limit', 'OOM']}},
  {volume: 'volume-01', match: /namespaces|cgroups|container/i, bridge: {label: 'Container foundation', problem: 'Several workloads must share one Linux kernel while seeing controlled resources and consuming bounded capacity.', normalPath: 'container request → runtime creates Linux processes → namespaces change visibility → cgroups account/limit resources → kernel enforces', terms: ['process', 'namespace', 'cgroup', 'container image', 'runtime', 'capability']}},
  {volume: 'volume-01', match: /systemd|boot|service|signal/i, bridge: {label: 'Service foundation', problem: 'A server needs a predictable way to boot, start long-running services, supervise them, stop them safely and retain evidence.', normalPath: 'firmware/bootloader → Linux kernel → systemd → unit dependencies → service process → journal and exit status', terms: ['unit', 'service', 'target', 'dependency', 'signal', 'exit status', 'journal']}},

  {volume: 'volume-03', match: /scheduler|resources|topology/i, bridge: {label: 'Scheduling foundation', problem: 'A pending Pod needs a suitable node, but suitability includes capacity, policy, placement and topology—not merely a free machine.', normalPath: 'unscheduled Pod → filtering eligible nodes → scoring candidates → binding → kubelet execution', terms: ['request', 'limit', 'capacity', 'allocatable', 'taint', 'toleration', 'affinity', 'binding']}},
  {volume: 'volume-03', match: /kubelet|cri|pod lifecycle/i, bridge: {label: 'Node-execution foundation', problem: 'After scheduling, something on the selected node must turn a Pod specification into real Linux processes and report their state.', normalPath: 'bound Pod → kubelet → container runtime interface → image/runtime → Pod sandbox and containers → probes/status', terms: ['kubelet', 'CRI', 'runtime', 'Pod sandbox', 'probe', 'restart policy']}},
  {volume: 'volume-03', match: /network|service|cni|dns|gateway/i, bridge: {label: 'Kubernetes networking foundation', problem: 'Pods change location and address, while applications need stable discovery and permitted packet paths.', normalPath: 'client name lookup → Service/endpoint selection → node/CNI dataplane → Pod interface → listening application', terms: ['Pod IP', 'Service', 'EndpointSlice', 'CNI', 'kube-proxy/dataplane', 'NetworkPolicy', 'DNS']}},
  {volume: 'volume-03', match: /storage|statefulset/i, bridge: {label: 'Kubernetes storage foundation', problem: 'Container filesystems are disposable, but databases and other stateful workloads need durable storage with lifecycle and placement rules.', normalPath: 'Pod claim → PVC → storage class/provisioner → PV/volume → node attach/mount → container path', terms: ['volume', 'PV', 'PVC', 'StorageClass', 'CSI', 'attach', 'mount', 'StatefulSet']}},
  {volume: 'volume-03', match: /security|authentication|rbac|identity|admission/i, bridge: {label: 'Kubernetes security foundation', problem: 'The platform must prove who is making a request, decide what they may do, constrain workloads and record decisions.', normalPath: 'request → authentication → authorization/RBAC → admission policy → persisted object → runtime security controls', terms: ['authentication', 'authorization', 'RBAC', 'ServiceAccount', 'admission', 'security context', 'secret']}},
  {volume: 'volume-03', match: /autoscal|capacity/i, bridge: {label: 'Scaling foundation', problem: 'Demand changes, so the platform needs measured signals and enough underlying capacity to add useful replicas or nodes.', normalPath: 'workload demand → metric → scaling policy → desired replicas/nodes → scheduling → warm-up/readiness → measured outcome', terms: ['replica', 'HPA', 'VPA', 'cluster autoscaler', 'metric', 'desired state', 'warm-up']}},
  {volume: 'volume-03', match: /upgrade|reliability|cluster operations/i, bridge: {label: 'Cluster-lifecycle foundation', problem: 'A Kubernetes cluster is a dependency graph whose components and workloads must continue operating through controlled change.', normalPath: 'compatibility review → control-plane change → limited node wave → drain/replace → workload validation → wider rollout', terms: ['version skew', 'drain', 'disruption budget', 'surge', 'canary', 'rollback']}},

  {volume: 'volume-04', match: /pcie|nvlink|nvswitch|topology|numa/i, bridge: {label: 'GPU-topology foundation', problem: 'Two visible devices can have very different communication paths, bandwidth, latency and CPU/NIC locality.', normalPath: 'CPU or peer GPU → PCIe/NVLink path → switches/root complexes → target GPU memory; topology determines the path', terms: ['PCIe', 'root complex', 'NUMA', 'NVLink', 'NVSwitch', 'locality', 'peer-to-peer']}},
  {volume: 'volume-04', match: /driver|cuda|runtime|container stack|compatibility/i, bridge: {label: 'GPU software-stack foundation', problem: 'Applications reach GPU hardware through several independently versioned layers that must remain compatible.', normalPath: 'framework/application → CUDA user-space libraries → NVIDIA driver → firmware → GPU hardware', terms: ['framework', 'CUDA runtime', 'library', 'driver', 'kernel module', 'firmware', 'compatibility']}},
  {volume: 'volume-04', match: /device plugin|gpu operator/i, bridge: {label: 'Kubernetes GPU-integration foundation', problem: 'Kubernetes cannot schedule a GPU safely until node software discovers devices, advertises resources and injects assigned devices into workloads.', normalPath: 'host driver/device → Operator-managed operands → device discovery/advertisement → scheduler allocation → runtime injection → container', terms: ['Operator', 'operand', 'device plugin', 'extended resource', 'allocatable', 'runtime injection']}},
  {volume: 'volume-04', match: /sharing|mig|time.slicing|mps|vgpu/i, bridge: {label: 'GPU-sharing foundation', problem: 'Expensive GPUs may need to serve more than one workload, but sharing choices trade isolation, predictability, flexibility and utilization.', normalPath: 'workload requirement → sharing policy → scheduler-visible resource → runtime/device enforcement → measured isolation and performance', terms: ['MIG', 'time-slicing', 'MPS', 'vGPU', 'isolation', 'oversubscription']}},
  {volume: 'volume-04', match: /telemetry|dcgm|xid|ecc|health/i, bridge: {label: 'GPU-health foundation', problem: 'Operators need to distinguish device presence, workload activity, correct operation, degradation and hardware failure.', normalPath: 'GPU/driver counters and events → DCGM/exporter → metrics/alerts → node/workload correlation → validation or quarantine', terms: ['telemetry', 'DCGM', 'Xid', 'ECC', 'counter', 'diagnostic', 'quarantine']}},

  {volume: 'volume-05', match: /training|parallelism|collective|checkpoint/i, bridge: {label: 'Training foundation', problem: 'Training repeatedly computes predictions, measures error and updates model weights, often across many synchronized GPUs.', normalPath: 'batch data → forward pass → loss → backward pass/gradients → collective synchronization → weight update → checkpoint', terms: ['model', 'weight', 'batch', 'loss', 'gradient', 'optimizer', 'collective', 'checkpoint']}},
  {volume: 'volume-05', match: /llm inference|prefill|decode|kv cache|continuous batch/i, bridge: {label: 'LLM-inference foundation', problem: 'A language-model service must turn variable-length requests into generated tokens while managing latency, batching and GPU memory.', normalPath: 'request → tokenize/queue → prefill → KV-cache state → repeated decode steps → streamed or completed response', terms: ['token', 'prompt', 'prefill', 'decode', 'KV cache', 'batching', 'TTFT', 'inter-token latency']}},
  {volume: 'volume-05', match: /serving framework|nim|vllm|tensorrt|dynamo|platform boundary/i, bridge: {label: 'Model-serving foundation', problem: 'A model artifact needs an engine and operational service around it: loading, request scheduling, batching, routing, health and observability.', normalPath: 'client → gateway/router → serving process/engine → model execution on GPU → response and metrics', terms: ['model artifact', 'serving engine', 'runtime', 'router', 'batcher', 'replica', 'endpoint']}},
  {volume: 'volume-05', match: /autoscal/i, bridge: {label: 'Inference-scaling foundation', problem: 'Inference capacity must respond to arriving work while accounting for queueing, warm-up, state and coarse GPU allocation.', normalPath: 'arrival/concurrency → queue and latency signals → scaling decision → GPU capacity → model load/warm-up → ready replica', terms: ['arrival rate', 'queue depth', 'concurrency', 'replica', 'warm-up', 'saturation', 'SLO']}},
  {volume: 'volume-05', match: /distributed|disaggregated/i, bridge: {label: 'Distributed-inference foundation', problem: 'A model or workload may exceed one GPU or benefit from separating stages with different compute and memory behavior.', normalPath: 'request routing → stage/parallel shard placement → cross-GPU communication or state transfer → coordinated response', terms: ['tensor parallelism', 'pipeline parallelism', 'shard', 'disaggregation', 'state transfer', 'topology']}},
  {volume: 'volume-05', match: /rag|vector|state|cache/i, bridge: {label: 'RAG and state foundation', problem: 'Generation may depend on external knowledge retrieval and caches, adding data freshness, authorization, latency and failure boundaries.', normalPath: 'query → embedding → retrieval/filtering → context construction → model inference → answer/citation', terms: ['embedding', 'vector index', 'retrieval', 'context', 'cache', 'freshness', 'tenant filter']}},
  {volume: 'volume-05', match: /security|tenancy|governance/i, bridge: {label: 'AI-security foundation', problem: 'AI platforms handle models, prompts, datasets and outputs with different owners, sensitivities and allowed uses.', normalPath: 'identity → request authorization → data/model access → isolated execution → output controls → audit', terms: ['tenant', 'trust boundary', 'data residency', 'model artifact', 'secret', 'policy', 'audit']}},
  {volume: 'volume-05', match: /performance|cost|benchmark/i, bridge: {label: 'Performance-and-cost foundation', problem: 'The cheapest or fastest component is not necessarily the system with the best cost per successful workload outcome.', normalPath: 'representative workload → defined quality/SLO → measured throughput and latency → resource consumption → cost per outcome', terms: ['baseline', 'throughput', 'latency', 'utilization', 'saturation', 'quality', 'TCO']}},

  {volume: 'volume-06', match: /ethernet/i, bridge: {label: 'Ethernet foundation', problem: 'Distributed systems need addressed, routed and switched packet delivery before any accelerated transport can work.', normalPath: 'application socket → host network stack → NIC → switch fabric → remote NIC → remote socket', terms: ['frame', 'packet', 'MAC', 'IP address', 'subnet', 'route', 'switch', 'MTU']}},
  {volume: 'volume-06', match: /rdma|roce|infiniband/i, bridge: {label: 'RDMA foundation', problem: 'Large distributed workloads need efficient low-latency data movement without repeated CPU copies and protocol overhead.', normalPath: 'registered memory → queue pair/transport → HCA/NIC → fabric → remote registered memory → completion', terms: ['RDMA', 'registered memory', 'HCA', 'queue pair', 'InfiniBand', 'RoCE', 'congestion']}},
  {volume: 'volume-06', match: /gpudirect|nic.gpu|nccl/i, bridge: {label: 'GPU-network-path foundation', problem: 'Cross-node GPU work depends on both logical communication libraries and the physical path between GPU, NIC and fabric.', normalPath: 'GPU buffer → PCIe/locality path → NIC/HCA → fabric → remote NIC → remote GPU buffer → collective completion', terms: ['GPUDirect RDMA', 'NCCL', 'collective', 'PCIe locality', 'rail', 'fallback']}},
  {volume: 'volume-06', match: /slurm/i, bridge: {label: 'Slurm foundation', problem: 'Many users need fair, controlled access to finite compute resources and a reliable way to launch batch work.', normalPath: 'job submission → policy/priority → resource allocation → node launch → job steps → completion/accounting', terms: ['job', 'partition', 'allocation', 'node', 'slurmctld', 'slurmd', 'GRES', 'accounting']}},
  {volume: 'volume-06', match: /kubernetes.slurm|slurm.or.both|hybrid schedul/i, bridge: {label: 'Scheduler-choice foundation', problem: 'Kubernetes and Slurm optimize different workload and operating models; a hybrid must assign clear ownership instead of letting both control the same resources.', normalPath: 'workload and operating requirements → scheduler boundary → dedicated resource ownership → shared service boundaries → validation', terms: ['service workload', 'batch job', 'queue', 'reconciliation', 'resource ownership', 'hybrid']}},

  {volume: 'volume-07', match: /metrics.logs.and.traces|different evidence/i, bridge: {label: 'Evidence foundation', problem: 'An incident produces many signals, but each evidence type answers a different question.', normalPath: 'user symptom → outcome metric → scoped component evidence → event/log/trace/profile → tested hypothesis', terms: ['metric', 'log', 'trace', 'event', 'profile', 'correlation']}},
  {volume: 'volume-07', match: /sli|slo|error budget/i, bridge: {label: 'Reliability-objective foundation', problem: 'Teams need an explicit definition of acceptable service behavior before alerts and reliability decisions can be meaningful.', normalPath: 'user journey → measurable SLI → target/window SLO → error budget → operational decision', terms: ['SLI', 'SLO', 'SLA', 'error budget', 'availability', 'latency']}},
  {volume: 'volume-07', match: /prometheus|promql/i, bridge: {label: 'Prometheus foundation', problem: 'Operators need time-series measurements that can be queried and aggregated consistently across systems.', normalPath: 'instrumented target/exporter → scrape → labeled time series → PromQL query/rule → dashboard or alert', terms: ['target', 'scrape', 'time series', 'label', 'counter', 'gauge', 'rate', 'cardinality']}},
  {volume: 'volume-07', match: /kubernetes observability/i, bridge: {label: 'Kubernetes-observability foundation', problem: 'Kubernetes desired/object state and actual process/workload behavior are different views that must be correlated.', normalPath: 'API object/status/event → scheduler/kubelet/runtime evidence → container/process metrics and logs → application outcome', terms: ['desired state', 'status', 'event', 'control plane', 'runtime', 'workload signal']}},
  {volume: 'volume-07', match: /gpu observability|dcgm/i, bridge: {label: 'GPU-observability foundation', problem: 'GPU presence and activity do not prove useful, correct or efficient workload execution.', normalPath: 'workload outcome → framework/engine metrics → DCGM/driver evidence → topology/host evidence → validated hypothesis', terms: ['DCGM', 'utilization', 'memory capacity', 'power', 'clock', 'Xid', 'ECC']}},
  {volume: 'volume-07', match: /logs/i, bridge: {label: 'Logging foundation', problem: 'Engineers investigating after the fact need event records with enough context to reconstruct what happened.', normalPath: 'application/component event → structured record → collection/transport → indexed storage → correlated query', terms: ['timestamp', 'severity', 'structured log', 'correlation ID', 'retention', 'redaction']}},
  {volume: 'volume-07', match: /alert|runbook/i, bridge: {label: 'Alerting foundation', problem: 'An alert should identify actionable service risk, not merely report every unusual metric.', normalPath: 'SLI or failure signal → evaluated rule/window → routed notification → runbook decision → mitigation and validation', terms: ['threshold', 'window', 'burn rate', 'severity', 'routing', 'runbook', 'actionability']}},
  {volume: 'volume-07', match: /incident communication|postmortem/i, bridge: {label: 'Incident-management foundation', problem: 'Technical recovery and clear coordination must happen together while facts are incomplete.', normalPath: 'impact detected → roles/scope → evidence and mitigation → validation → communication → blameless learning/actions', terms: ['incident commander', 'impact', 'timeline', 'mitigation', 'resolution', 'postmortem']}},

  {volume: 'volume-08', match: /discovery/i, bridge: {label: 'Discovery foundation', problem: 'Customers often present a preferred solution before the underlying workload, outcome and constraints are understood.', normalPath: 'business outcome → workload/SLO → current state → constraints/risks → unknowns → decision criteria', terms: ['outcome', 'requirement', 'constraint', 'assumption', 'stakeholder', 'SLO']}},
  {volume: 'volume-08', match: /architecture|data.and.control.paths/i, bridge: {label: 'Architecture-modeling foundation', problem: 'A useful architecture explains how work, data, control and trust move—not merely which products exist.', normalPath: 'requirements → request/data/control/trust paths → state and failure domains → options → validated recommendation', terms: ['data path', 'control plane', 'state', 'trust boundary', 'failure domain', 'dependency']}},
  {volume: 'volume-08', match: /trade.off|weighted requirement/i, bridge: {label: 'Trade-off foundation', problem: 'Several options may work, so a recommendation needs explicit criteria and consequences rather than preference.', normalPath: 'requirements → weighted criteria → feasible options → evidence/assumptions → sensitivity → recommendation', terms: ['criterion', 'weight', 'option', 'trade-off', 'assumption', 'sensitivity']}},
  {volume: 'volume-08', match: /poc|proof.of.concept/i, bridge: {label: 'PoC foundation', problem: 'A proof of concept should reduce a decision-critical uncertainty, not simply show that software installs.', normalPath: 'uncertain claim → representative test → acceptance threshold → controlled execution → result → decision', terms: ['hypothesis', 'representative workload', 'baseline', 'acceptance criterion', 'reproducibility']}},
  {volume: 'volume-08', match: /tco|capacity/i, bridge: {label: 'Capacity-and-cost foundation', problem: 'Capacity is derived from workload demand and service objectives; total cost includes operations and risk, not only purchase price.', normalPath: 'demand distribution/SLO → benchmark capacity → headroom/failure model → resource count → capital and operating cost', terms: ['capacity', 'headroom', 'utilization', 'throughput', 'TCO', 'sensitivity']}},
  {volume: 'volume-08', match: /migration|adoption/i, bridge: {label: 'Migration foundation', problem: 'A technically sound target fails if workloads, people, data and operations cannot move safely to it.', normalPath: 'current-state inventory → dependency/risk groups → pilot → migration waves → validation → decommission/operating handoff', terms: ['current state', 'target state', 'wave', 'rollback', 'adoption', 'operating model']}},
];

const volumeFallbacks: Record<string, ChapterFoundationBridge> = {
  'volume-01': {label: 'Linux prerequisite bridge', problem: 'This chapter examines a Linux mechanism in more depth than a first-time reader may have seen.', normalPath: 'start with the user-visible symptom → identify process/kernel/hardware boundary → observe read-only evidence → then study internals', terms: ['process', 'kernel', 'resource', 'system call', 'evidence']},
  'volume-02': {label: 'Python prerequisite bridge', problem: 'This chapter builds production behavior on ordinary Python values, functions and explicit effects.', normalPath: 'input → validated values → decision function → external effect → result/error/exit code → test', terms: ['value', 'function', 'return value', 'exception', 'effect', 'test']},
  'volume-03': {label: 'Kubernetes prerequisite bridge', problem: 'This chapter extends Kubernetes desired-state reconciliation into a specialized control or runtime boundary.', normalPath: 'API intent → stored object → controller/scheduler decision → node/runtime action → status and workload outcome', terms: ['API object', 'desired state', 'controller', 'node', 'status']},
  'volume-04': {label: 'GPU prerequisite bridge', problem: 'This chapter adds depth to the host-software-to-GPU execution and data path.', normalPath: 'application → CUDA user space → driver → GPU execution/memory → topology or workload outcome', terms: ['driver', 'CUDA', 'device memory', 'GPU kernel', 'topology']},
  'volume-05': {label: 'AI-workload prerequisite bridge', problem: 'This chapter makes sense only after the workload and measurable outcome are clear.', normalPath: 'workload/request/data → model execution → state/communication → output → quality, latency or throughput result', terms: ['model', 'training', 'inference', 'batch', 'SLO']},
  'volume-06': {label: 'HPC prerequisite bridge', problem: 'This chapter adds a distributed dependency to an already-working single-node workload.', normalPath: 'scheduler allocation → rank launch → local compute → network/collective or storage path → synchronized completion', terms: ['job', 'rank', 'scheduler', 'collective', 'fabric']},
  'volume-07': {label: 'Reliability prerequisite bridge', problem: 'This chapter turns system evidence into a decision about user impact, diagnosis or safe response.', normalPath: 'service objective → symptom/scope → correlated evidence → hypothesis → mitigation → validation', terms: ['SLI', 'scope', 'evidence', 'hypothesis', 'validation']},
  'volume-08': {label: 'Architecture prerequisite bridge', problem: 'This chapter converts customer uncertainty into an explicit, testable decision.', normalPath: 'outcome → requirements/constraints → paths and failure domains → options/trade-offs → recommendation and validation', terms: ['requirement', 'constraint', 'trade-off', 'assumption', 'validation']},
  'volume-09': {label: 'Study prerequisite', problem: 'This page assesses knowledge; it is not intended to be the first explanation of the technical topic.', normalPath: 'attempt in your own words → identify missing nouns/normal path → return to the core volume → observe/practice → answer again', terms: ['scope', 'normal path', 'evidence', 'trade-off', 'validation']},
  'volume-10': {label: 'Cross-layer prerequisite bridge', problem: 'This operational chapter combines hardware, Linux, automation, scheduling and distributed-workload boundaries.', normalPath: 'physical/lifecycle state → OS/configuration → health eligibility → allocation/launch → workload outcome → controlled change', terms: ['desired state', 'readiness', 'scheduler', 'failure domain', 'rollback']},
};

export function getChapterFoundationBridge(title: string, route: string): ChapterFoundationBridge | undefined {
  const volume = route.match(/volume-\d{2}/)?.[0];
  if (!volume) return undefined;
  return rules.find((rule) => rule.volume === volume && rule.match.test(title))?.bridge || volumeFallbacks[volume];
}

