export type InterviewQuestion = {id: string; category: string; question: string; followUp: string; framework: string; answer: string};
export const interviewQuestions: InterviewQuestion[] = [
  ['linux-1','Linux','A host shows high load average but moderate CPU utilization. How do you investigate?','How do runnable and uninterruptible tasks differ?','Clarify impact → inspect run queue and blocked tasks → correlate CPU, memory, storage, and cgroup evidence → test a hypothesis.','Load includes runnable and uninterruptible work. I would scope affected workloads, use vmstat/pidstat/ps to separate run-queue pressure from I/O waits, inspect cgroup throttling and storage latency, then validate the leading cause before mitigation.'],
  ['python-1','Python','Design a safe Python wrapper around kubectl for production diagnostics.','How do you test timeout and malformed-output paths?','Define typed input/output → argv without shell → timeout → capture streams → classify errors → unit-test decisions.','Use subprocess.run with an argv list, shell=False, a bounded timeout, text capture, and explicit return-code handling. Parse structured JSON, return a typed result, redact secrets, log correlation fields, and mock the process boundary in tests.'],
  ['k8s-1','Kubernetes','Walk through what happens after a Pod is submitted.','Where do admission, scheduling, and CRI fit?','API lifecycle → persistence/watch → scheduling → kubelet sync → sandbox/runtime → readiness/endpoints.','The API server authenticates and authorizes, runs admission, validates and persists the object in etcd. Controllers observe it; the scheduler filters/scores and binds. Kubelet sees the assigned Pod, uses CRI to create its sandbox and containers, then reports status; readiness controls traffic eligibility.'],
  ['gpu-1','GPU','A node is Ready but exposes no nvidia.com/gpu. Diagnose it.','How do driver, toolkit, and device plugin responsibilities differ?','Resource advertisement chain → device-plugin evidence → NVML → driver/kernel → operator state.','I would confirm allocatable state and device-plugin health, then check its NVML errors, host nvidia-smi, kernel logs, and GPU Operator operands. I would cordon the node before changing the driver stack and validate a known-good CUDA workload before returning it.'],
  ['inference-1','AI inference','Explain TTFT versus TPOT and how you would optimize each.','What does continuous batching change?','Decompose request queue/tokenize/prefill/decode → map each metric to resource and scheduler behavior.','TTFT covers admission, queueing and prefill before the first token; TPOT/ITL measures decode cadence. Prefill is compute-heavy and prompt-length sensitive, while decode is memory/KV-cache sensitive. I would segment metrics by prompt/output length and tune batching, concurrency and prefill/decode scheduling against SLOs.'],
  ['training-1','Distributed training','Why can adding GPUs reduce scaling efficiency?','Compare AllReduce ring and tree intuition.','Compute/communication ratio → collective volume → topology → stragglers → measurement.','More ranks increase synchronization and communication. Slow ranks, oversubscribed links, remote topology, small per-GPU batches, and storage stalls are amplified at barriers. I would compare single-node baseline, per-rank timelines, NCCL/fabric evidence, and placement before recommending topology or parallelism changes.'],
  ['network-1','RDMA/RoCE/InfiniBand','Compare InfiniBand and RoCE for an AI fabric.','What must be true for GPUDirect RDMA?','Start from workload/SLO → operational model → congestion/loss → topology → existing skills/cost.','Both can provide RDMA. InfiniBand is an integrated fabric with its own management model; RoCE carries RDMA over Ethernet and demands disciplined end-to-end congestion/loss configuration. The choice depends on scale, operational maturity, failure isolation, existing fabric, and measured collective needs—not a protocol slogan.'],
  ['storage-1','Storage','Design the data path for a 256-GPU training cluster.','How do checkpoints change the design?','Dataset shape/access → aggregate bandwidth/metadata → tiers/cache → checkpoint burst → failure domains.','I would quantify samples/sec, file sizes, metadata rate and checkpoint RPO. Use durable object/parallel storage, a high-throughput shared tier, and node/local cache or sharding where useful. Validate aggregate reads and synchronized checkpoint writes, recovery time, namespace pressure, and network contention.'],
  ['observability-1','Observability','What would you put in a GPU platform SLO and dashboard?','Which alerts page versus ticket?','User outcome → SLIs → dependencies → saturation/health → error budget/action.','Start with workload admission/start success, training progress or inference latency/availability. Add scheduler queue, node health, GPU duty/memory/ECC/Xid, fabric, storage and service saturation as diagnostic signals. Page on actionable user impact; ticket capacity and drift trends.'],
  ['architecture-1','Architecture','Whiteboard a multi-tenant enterprise inference platform.','Where are the strongest isolation boundaries?','Discover → requirements/unknowns → data/control paths → compute/network/storage → tenancy/security → observability/HA/cost.','I would clarify models, traffic, TTFT/TPOT, data sensitivity and tenancy. Then separate gateway/control plane from GPU serving pools; use workload identity, quotas, policy and model provenance; select serving/runtime by model; design KV/cache behavior, autoscaling from queue/token work, observability, rollout and failure domains.'],
  ['discovery-1','Customer discovery','A customer asks for “the fastest GPUs.” What do you ask first?','How do you turn answers into a PoC?','Outcome → workload → SLO → dataset/model → constraints → operations → economics.','I would ask whether this is training or inference, model sizes/precision, concurrency and latency/throughput targets, data locality, framework, scale, availability, security, facilities and operating model. Those answers determine whether GPU compute, memory, interconnect, storage or software is the binding constraint.'],
  ['poc-1','PoC','How do you design an AI infrastructure PoC?','What prevents a demo from becoming evidence?','Decision/uncertainty → hypothesis → representative workload → baseline → acceptance thresholds → telemetry → exit decision.','A PoC tests the riskiest assumptions with representative data and scale. I define success and failure thresholds up front, control variables, retain full telemetry, include failure/recovery and operations, compare a baseline, and finish with a decision and remaining uncertainty—not just a successful demo.'],
  ['tco-1','TCO/capacity','How do you estimate inference GPU capacity?','How do utilization and headroom enter the model?','Demand distribution → per-request work → measured service rate → concurrency/KV → SLO headroom → HA/growth → sensitivity.','Use benchmarked throughput at the target model, precision, prompt/output mix and latency SLO. Convert forecast traffic into GPU demand, then add failure-domain, rollout, burst and growth headroom. Report scenarios and sensitivity rather than false precision.'],
  ['behavior-1','Behavioral/leadership','Tell me about an incident where you changed the team’s technical approach.','What did you personally decide and what would you change?','Situation → stakes → evidence → your decision/influence → trade-off → measured result → reflection.','A strong answer makes ownership and constraints explicit, shows how evidence changed the decision, describes stakeholder alignment and safe execution, quantifies the result, and names a specific lesson—not just the team’s activity.'],
].map(([id, category, question, followUp, framework, answer]) => ({id, category, question, followUp, framework, answer}));
