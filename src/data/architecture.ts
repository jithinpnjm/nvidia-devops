export type ArchitectureCategory =
  | 'GPU platform'
  | 'Training & HPC'
  | 'Inference & GenAI'
  | 'Reliability & operations'
  | 'Security & edge'
  | 'Migration & economics'
  | 'Cloud & Kubernetes'
  | 'CI/CD & platform engineering';

export type ArchitectureScenario = {
  id: string;
  title: string;
  category: ArchitectureCategory;
  difficulty: 'Senior' | 'Staff';
  duration: number;
  brief: string;
  ask: string;
  requirements: string[];
  unknowns: string[];
  constraints: string[];
  answerOutline: {label: string; detail: string}[];
  tradeoffs: {decision: string; recommendation: string; alternative: string}[];
  failureModes: {failure: string; response: string}[];
  successMetrics: string[];
  followUps: string[];
  strongSignals: string[];
};

export const architectureScenarios: ArchitectureScenario[] = [
  {
    id: 'multi-tenant-gpu-platform',
    title: 'Multi-tenant GPU platform',
    category: 'GPU platform', difficulty: 'Senior', duration: 45,
    brief: 'A global engineering company has 12 ML teams sharing 128 mixed-generation GPUs. Notebook users complain about queue time while overnight training leaves expensive capacity idle. Security requires isolation between three regulated business units.',
    ask: 'Design the target platform and the operating model. Explain how workloads are admitted, placed, isolated, measured, upgraded and charged back.',
    requirements: ['Interactive notebooks start within 10 minutes', 'Batch training supports multi-GPU jobs', 'Per-team quota, priority and showback', 'Regulated tenants cannot share a security boundary'],
    unknowns: ['Workload shapes, GPU memory needs and duration distribution', 'Trust boundaries: namespace, cluster, host or physical pool', 'Expected burst profile and preemption tolerance', 'Current identity, Kubernetes and data platforms'],
    constraints: ['Finite premium GPUs and mixed hardware generations', 'Kubernetes is the preferred user API', 'Platform team has six engineers', 'No disruption to current production jobs during migration'],
    answerOutline: [
      {label: 'Workload contract', detail: 'Classify interactive, inference and batch jobs by GPU count, memory, topology, priority, duration and checkpointability before selecting a sharing mechanism.'},
      {label: 'Physical pools', detail: 'Separate regulated and trusted pools first; within a pool, use labels, taints, topology-aware scheduling and health gates. Avoid pretending namespace policy creates a hardware trust boundary.'},
      {label: 'Allocation', detail: 'Use full GPUs for performance-sensitive training, MIG for compatible hard partitions, and time-slicing only for low-duty-cycle trusted workloads after benchmarking.'},
      {label: 'Platform controls', detail: 'Federated identity, workload identity, quota, priority classes, admission policy, signed images, network policy and secrets integration form the control plane.'},
      {label: 'Day-2 operations', detail: 'Correlate tenant, workload, node and GPU UUID in telemetry; publish queue time, useful utilization, failure rate and cost; drain and validate nodes through staged lifecycle waves.'},
    ],
    tradeoffs: [
      {decision: 'Shared vs dedicated clusters', recommendation: 'Shared clusters inside compatible trust/performance boundaries', alternative: 'Dedicated clusters or physical pools when compliance or noisy-neighbor risk dominates'},
      {decision: 'Utilization vs predictability', recommendation: 'Explicit service classes with quotas and preemption', alternative: 'Static reservations for teams that require deterministic capacity'},
      {decision: 'MIG vs time-slicing', recommendation: 'MIG where the GPU/model supports the required profile and isolation', alternative: 'Time-slicing for trusted, bursty workloads that tolerate weak isolation'},
    ],
    failureModes: [
      {failure: 'A team requests a topology-constrained 8-GPU job but only fragmented capacity remains', response: 'Gang scheduling, topology-aware placement, reservation/defragmentation policy and bounded queue-time SLO.'},
      {failure: 'A bad driver rollout removes GPUs from allocatable capacity', response: 'Canary by hardware cohort, health-gated waves, automatic halt, known-good rollback and spare capacity.'},
      {failure: 'Showback reports allocated GPUs but not productive work', response: 'Combine allocation-hours with DCGM activity, job outcome and queue metrics; never equate busy samples with useful model throughput.'},
    ],
    successMetrics: ['p95 queue time by service class', 'Useful GPU-hours / available GPU-hours', 'Job completion and preemption recovery rate', 'Cost per successful training run or 1M inference tokens'],
    followUps: ['Budget is cut 30%; what do you protect first?', 'One regulated team now requires physical host isolation.', 'Demand doubles for six months but the preferred GPU SKU is unavailable.'],
    strongSignals: ['Starts with workload and trust-boundary discovery', 'Separates physical allocation from logical policy', 'Names a measurable fairness and utilization model', 'Includes migration and day-2 ownership'],
  },
  {
    id: 'distributed-training-256',
    title: '256-GPU distributed training environment',
    category: 'Training & HPC', difficulty: 'Staff', duration: 60,
    brief: 'A research organization must train a large model across 32 eight-GPU nodes. A single run lasts days, checkpoints every 20 minutes, and currently loses substantial time to stragglers and failed restarts.',
    ask: 'Design compute, topology, fabric, storage, scheduling and recovery. Show the training data path and quantify acceptance criteria.',
    requirements: ['Repeatable scaling to 256 GPUs', 'RPO no worse than 20 minutes', 'Node maintenance without draining the entire estate', 'Reproducible performance across runs'],
    unknowns: ['Parallelism strategy and collective communication profile', 'Dataset size, access pattern and preprocessing path', 'Checkpoint size, duration and restore bandwidth', 'Tolerance for elastic or reduced-size training'],
    constraints: ['Fixed facility power and fabric budget', 'Shared storage also serves other research workloads', 'Procurement lead time limits spare nodes'],
    answerOutline: [
      {label: 'Benchmark first', detail: 'Establish single-GPU, single-node and multi-node baselines; measure compute, collective and input pipeline separately before sizing the cluster.'},
      {label: 'Topology', detail: 'Keep ranks inside high-bandwidth GPU domains where possible, map NIC/GPU/NUMA locality, and place the job as an atomic topology-aware gang.'},
      {label: 'Networks', detail: 'Separate out-of-band management, in-band services, compute fabric and storage paths; validate bandwidth, latency, MTU and collective behavior end to end.'},
      {label: 'Storage path', detail: 'Stage datasets to a high-throughput tier or node cache, write checkpoints without congesting collectives, and replicate completed checkpoints to the durable tier.'},
      {label: 'Recovery', detail: 'Health-qualify nodes before admission, quarantine degradations, preserve scheduler state, and test restore time—not merely checkpoint creation.'},
    ],
    tradeoffs: [
      {decision: 'InfiniBand vs RoCE', recommendation: 'Choose from required scale, existing operational skills and validated end-to-end design', alternative: 'RoCE can fit an Ethernet estate but requires disciplined congestion, QoS and telemetry operations'},
      {decision: 'Frequent checkpoints vs throughput', recommendation: 'Derive interval from failure rate, write time and lost-work cost', alternative: 'Less frequent checkpoints if the storage pause costs more than expected recompute'},
      {decision: 'Slurm vs Kubernetes', recommendation: 'Use the scheduler whose topology, gang and research workflow match the workload', alternative: 'A bounded hybrid model if services and batch genuinely need different control planes'},
    ],
    failureModes: [
      {failure: 'One rail silently falls back from GPUDirect RDMA', response: 'Compare per-node topology and NCCL evidence, quarantine the asymmetric node and validate the repaired path with a collective benchmark.'},
      {failure: 'Checkpoint burst saturates storage and stalls every rank', response: 'Isolate the storage path, stagger or async-write checkpoints, use local buffering and enforce a checkpoint completion SLO.'},
      {failure: 'One slow node stretches every iteration', response: 'Detect per-rank skew, correlate GPU clocks/ECC, NIC counters, CPU/NUMA and I/O, then health-gate the node.'},
    ],
    successMetrics: ['Scaling efficiency versus validated baseline', 'Iteration-time variance and straggler rate', 'Checkpoint write and restore time', 'Goodput: successful training progress / reserved GPU-hours'],
    followUps: ['The job must scale to 1,024 GPUs next year.', 'Storage budget is halved.', 'Two nodes fail midway through a weekend run.'],
    strongSignals: ['Builds a performance model before naming products', 'Draws separate management, compute and storage paths', 'Treats topology and health as scheduler inputs', 'Measures goodput rather than raw utilization'],
  },
  {
    id: 'enterprise-inference',
    title: 'Enterprise LLM inference platform',
    category: 'Inference & GenAI', difficulty: 'Staff', duration: 60,
    brief: 'Eight business units need private LLM endpoints for models from 8B to 70B parameters. Traffic is bursty, prompts vary from hundreds to tens of thousands of tokens, and some requests contain confidential data.',
    ask: 'Design a multi-tenant serving platform with latency SLOs, safe releases, cost controls and a clear request path.',
    requirements: ['p95 TTFT under 2 seconds for the interactive tier', 'Streaming responses and bounded queueing', 'Per-tenant identity, policy and cost attribution', 'Zero-downtime model and runtime rollout'],
    unknowns: ['Prompt/output length distribution and concurrency', 'Model precision, quality floor and residency policy', 'Availability target and acceptable load shedding', 'Whether prefix/KV reuse is safe across tenants'],
    constraints: ['Private connectivity only', 'Finite GPU capacity per region', 'Models have different GPU and memory footprints'],
    answerOutline: [
      {label: 'SLO contract', detail: 'Define TTFT, inter-token latency, end-to-end latency, throughput and quality by service tier; capacity from measured traffic distributions, not average RPS.'},
      {label: 'Request path', detail: 'Identity-aware gateway performs auth, quota and routing; bounded queues feed model-specific serving pools with continuous batching and topology-aware placement.'},
      {label: 'Model path', detail: 'Signed artifacts move from registry through validation and optimization into immutable model/cache storage, then roll out by canary with automated SLO rollback.'},
      {label: 'Capacity', detail: 'Maintain warm minimums, scale on waiting work or inference metrics, separate incompatible workload shapes, and define overload admission plus graceful degradation.'},
      {label: 'Operations', detail: 'Trace request-to-model-version and expose TTFT, ITL, tokens/s, queue depth, cache efficiency, errors, GPU health and cost per tenant.'},
    ],
    tradeoffs: [
      {decision: 'Throughput vs latency', recommendation: 'Bound batching and queue delay per service tier', alternative: 'Larger batches for offline or cost-optimized tiers'},
      {decision: 'Shared vs dedicated model replicas', recommendation: 'Share only where data policy and workload interference permit', alternative: 'Dedicated pools for regulated or latency-critical tenants'},
      {decision: 'Scale-to-zero vs warm capacity', recommendation: 'Warm capacity for strict TTFT tiers', alternative: 'Scale-to-zero for infrequent asynchronous workloads with relaxed startup SLO'},
    ],
    failureModes: [
      {failure: 'A burst fills the queue while GPU utilization appears moderate', response: 'Inspect prefill/decode mix, queue age, KV pressure and per-model saturation; enforce admission and tier-aware load shedding.'},
      {failure: 'A new engine version improves throughput but regresses long prompts', response: 'Canary against a representative prompt matrix and roll back on TTFT/ITL/quality guardrails.'},
      {failure: 'Model weights are unavailable during a node replacement', response: 'Use replicated durable model storage plus local cache warming and readiness that includes model load.'},
    ],
    successMetrics: ['TTFT and inter-token latency by prompt percentile', 'Tokens/s/GPU at the agreed SLO', 'Queue age, rejection rate and availability', 'Cost per 1M input/output tokens'],
    followUps: ['Traffic rises 10x for a product launch.', 'The 70B model must span multiple nodes.', 'A tenant demands data-plane isolation and customer-managed keys.'],
    strongSignals: ['Sizes from token distributions rather than CPU or average RPS', 'Separates request, model and control paths', 'Defines overload behavior explicitly', 'Treats quality and cost as guardrails beside latency'],
  },
  {
    id: 'rag-platform',
    title: 'Secure enterprise RAG platform',
    category: 'Inference & GenAI', difficulty: 'Senior', duration: 45,
    brief: 'A company wants grounded answers across millions of documents in multiple repositories. Document permissions change often, deleted content must disappear quickly, and every answer must be auditable.',
    ask: 'Design ingestion, retrieval, generation and evaluation paths while preserving source authorization.',
    requirements: ['Freshness under 30 minutes', 'Document-level ACL enforcement', 'Citation and version traceability', 'Tenant and data-residency isolation'],
    unknowns: ['Corpus size, formats and update/delete rates', 'Identity mapping across source systems', 'Quality targets and evaluation dataset', 'Embedding and reranking latency budget'],
    constraints: ['Sensitive documents cannot enter public services', 'Existing source systems remain authoritative', 'Deletion SLA is legally binding'],
    answerOutline: [
      {label: 'Ingestion path', detail: 'Change events trigger extraction, classification, chunking, embedding and versioned indexing; tombstones propagate deletes and failed stages enter a replayable queue.'},
      {label: 'Serving path', detail: 'Authenticate first, resolve user entitlements, apply ACL filters during retrieval, rerank bounded candidates, then generate with citations to immutable source versions.'},
      {label: 'Isolation', detail: 'Partition indexes, keys and processing pools according to the actual tenant trust boundary; never rely only on prompt instructions for authorization.'},
      {label: 'Evaluation', detail: 'Continuously test retrieval recall, groundedness, citation correctness, refusal and leakage using a versioned representative set.'},
      {label: 'Audit', detail: 'Record user, policy decision, query, retrieved document/version IDs, model/prompt version and outcome without logging prohibited content.'},
    ],
    tradeoffs: [
      {decision: 'Shared vs per-tenant index', recommendation: 'Choose from tenant count, ACL complexity and trust boundary', alternative: 'Shared index with strict metadata filtering only when independently tested for leakage'},
      {decision: 'Freshness vs indexing cost', recommendation: 'Event-driven incremental updates with periodic reconciliation', alternative: 'Batch rebuilds for stable low-priority corpora'},
      {decision: 'Retrieval depth vs latency', recommendation: 'Tune retrieval and reranking against measured recall/latency curves', alternative: 'Deeper retrieval for offline analytical workflows'},
    ],
    failureModes: [
      {failure: 'A user loses access but stale chunks remain retrievable', response: 'Evaluate authorization at query time, propagate tombstones, track entitlement/index lag and block stale partitions when policy requires.'},
      {failure: 'Poisoned documents manipulate answers', response: 'Source allowlists, content scanning, provenance, retrieval controls and output evaluation; retrieved text remains untrusted input.'},
      {failure: 'Embedding migration creates mixed incompatible vectors', response: 'Version indexes and embedding pipelines, dual-write/validate, then atomically switch retrieval.'},
    ],
    successMetrics: ['ACL leakage rate: zero in adversarial tests', 'p95 source-to-search freshness and delete lag', 'Retrieval recall and grounded-answer rate', 'End-to-end latency and cost per query'],
    followUps: ['One repository has no change feed.', 'The same document has conflicting ACLs in two sources.', 'Users require conversations that retain context for 30 days.'],
    strongSignals: ['Treats retrieval as an authorization boundary', 'Separates ingestion and serving failure domains', 'Designs deletion and reconciliation explicitly', 'Defines measurable answer-quality evaluation'],
  },
  {
    id: 'hybrid-kubernetes-slurm',
    title: 'Hybrid Kubernetes + Slurm estate',
    category: 'Training & HPC', difficulty: 'Staff', duration: 60,
    brief: 'A mature Slurm research environment must add Kubernetes-native inference and MLOps without disrupting batch users. Leadership asks for one shared GPU fleet and one common user experience.',
    ask: 'Decide the scheduler boundary, shared services and capacity-transfer model. Challenge the assumption that every GPU can be safely dual-owned.',
    requirements: ['Preserve existing research workflows and accounting', 'Launch production inference on Kubernetes', 'Common identity, artifacts and observability', 'Predictable capacity for both organizations'],
    unknowns: ['Workload split and future demand', 'Run:ai or other queueing capabilities in the Kubernetes path', 'Operator skill and on-call ownership', 'Whether nodes can be reprovisioned between pools'],
    constraints: ['Limited platform headcount', 'No double-booking or competing node agents', 'Existing Slurm QoS policy is politically important'],
    answerOutline: [
      {label: 'Workload placement', detail: 'Use Slurm for tightly coupled batch/research where its workflow is established; Kubernetes for long-running services and cloud-native delivery. Prove exceptions with workload evidence.'},
      {label: 'Ownership boundary', detail: 'Start with hard node pools. A capacity broker may transfer drained, health-checked nodes through an explicit reprovisioning state machine.'},
      {label: 'Shared layers', detail: 'Federate identity, artifact/model registry, dataset access, secrets policy, telemetry vocabulary and cost reporting without forcing one scheduler API.'},
      {label: 'Lifecycle', detail: 'Maintain one hardware/firmware/driver compatibility inventory and coordinated validation pipeline even when scheduler images differ.'},
      {label: 'Adoption', detail: 'Pilot non-critical inference, document golden paths, cross-train operators and publish ownership/RACI before expanding the boundary.'},
    ],
    tradeoffs: [
      {decision: 'One scheduler vs two', recommendation: 'Two bounded schedulers when workloads and operating models genuinely differ', alternative: 'Consolidate only after proving the target meets queueing, topology and service requirements'},
      {decision: 'Static vs dynamic pools', recommendation: 'Static pools first for operational clarity', alternative: 'Dynamic transfer after drain, reprovision, health and accounting automation is trustworthy'},
      {decision: 'Common image vs scheduler-specific image', recommendation: 'Common validated base with scheduler-specific layers', alternative: 'Independent images if release coupling creates excessive risk'},
    ],
    failureModes: [
      {failure: 'A transferred node retains stale agents from the prior scheduler', response: 'Reprovision from an immutable image, verify exclusive ownership and admit only after automated health/agent checks.'},
      {failure: 'Inference demand consumes research capacity before a deadline', response: 'Capacity envelopes, advance reservations, explicit transfer approvals and degraded-mode policy.'},
      {failure: 'Two telemetry systems disagree on GPU attribution', response: 'Normalize GPU UUID, node identity, job/workload and tenant labels at ingestion; reconcile allocation against scheduler state.'},
    ],
    successMetrics: ['Queue time and SLO attainment per scheduler', 'Capacity-transfer lead time and failure rate', 'GPU goodput by workload class', 'Operational toil and incident ownership ambiguity'],
    followUps: ['Leadership mandates 20% dynamic capacity transfer each day.', 'Kubernetes inference needs multi-node models.', 'The Slurm team refuses a shared driver release cadence.'],
    strongSignals: ['Rejects unsafe simultaneous ownership', 'Makes scheduler choice workload-led', 'Defines a node transfer state machine', 'Addresses people and operations, not only technology'],
  },
  {
    id: 'greenfield-ai-factory',
    title: 'Greenfield on-prem AI factory',
    category: 'Training & HPC', difficulty: 'Staff', duration: 60,
    brief: 'A customer has an empty data-center hall and a fixed date to deliver a production GPU cluster for training and inference. Compute, power, cooling, network, storage and operating ownership are being purchased by different teams.',
    ask: 'Create a dependency-aware Day 0 to Day 2 design and acceptance plan. Make clear what must be validated before the next layer begins.',
    requirements: ['Production-ready cluster by the committed date', 'HA management plane and controlled user access', 'Repeatable provisioning and configuration', 'Five-year support, capacity and upgrade plan'],
    unknowns: ['Workload mix and growth curve', 'Power/cooling readiness and rack density', 'Fabric/cabling and storage performance targets', 'Support demarcation across customer, integrator and vendors'],
    constraints: ['Long hardware lead times', 'Facility and platform teams have separate milestones', 'Limited opportunity for large-scale re-cabling after go-live'],
    answerOutline: [
      {label: 'Discovery and bill of design', detail: 'Translate workload, SLO, growth and compliance into compute, fabric, storage, management and facility envelopes plus explicit assumptions.'},
      {label: 'Dependency order', detail: 'Facility and OOB first; firmware/BMC inventory; management HA and provisioning; fabrics; storage; compute burn-in; scheduler/platform; workloads.'},
      {label: 'Network separation', detail: 'Design OOB management, in-band services, high-speed compute and storage paths with failure domains, addressing, MTU/QoS and observability.'},
      {label: 'Acceptance', detail: 'Validate per rack and at scale: inventory, thermals, GPU diagnostics, fabric, storage, collectives, scheduler placement, failure recovery and representative workloads.'},
      {label: 'Handover', detail: 'Deliver source-controlled configuration, golden images, compatibility matrix, spares, RACI, runbooks, SLO dashboards and a rehearsed upgrade procedure.'},
    ],
    tradeoffs: [
      {decision: 'Maximum initial density vs facility margin', recommendation: 'Preserve verified power/cooling and growth headroom', alternative: 'Higher density only with measured facility capacity and contained failure domains'},
      {decision: 'Turnkey stack vs composable tools', recommendation: 'Favor validated integrated lifecycle when schedule and staff are constrained', alternative: 'Composable stack when customer has strong ownership and specific integration needs'},
      {decision: 'Big-bang vs rack waves', recommendation: 'Rack-level waves with acceptance gates', alternative: 'Big-bang only when physical dependencies make staged value impossible'},
    ],
    failureModes: [
      {failure: 'Compute arrives before cooling is commissioned', response: 'Keep milestone dependencies explicit; do not accept or burn in systems outside environmental limits.'},
      {failure: 'Collective tests pass per rack but fail at full scale', response: 'Add multi-rack fabric and contention tests as a separate acceptance gate before workload sign-off.'},
      {failure: 'Day-2 team inherits undocumented vendor defaults', response: 'Configuration-as-code, exported baselines, drift checks, ownership sign-off and operational shadowing before acceptance.'},
    ],
    successMetrics: ['Acceptance pass rate by rack and layer', 'Measured workload goodput and fabric/storage headroom', 'Failure detection and repair time', 'Capacity, power and cooling margin'],
    followUps: ['Power delivery slips eight weeks but go-live does not move.', 'Half the storage throughput is available at launch.', 'The customer now requires both Slurm and Kubernetes.'],
    strongSignals: ['Starts at facility and workload constraints', 'Sequences dependencies instead of drawing only a logical stack', 'Defines measurable acceptance gates', 'Ends with a support and ownership model'],
  },
  {
    id: 'gpu-observability',
    title: 'GPU fleet observability and auto-remediation',
    category: 'Reliability & operations', difficulty: 'Senior', duration: 45,
    brief: 'A 500-node GPU fleet has dashboards full of metrics, yet operators cannot quickly connect failed jobs to Xid events, fabric degradation, recent changes or a specific physical component.',
    ask: 'Design the telemetry model, SLOs, alerting and safe remediation loop without creating dangerous automation or unbounded cardinality.',
    requirements: ['Fleet, node, GPU and workload correlation', 'Actionable alerts and incident evidence', 'Fast quarantine for known-bad devices', 'Long-term capacity and reliability reporting'],
    unknowns: ['Existing metrics/logs/traces backends', 'Scheduler and workload identity model', 'Which faults are safely auto-remediable', 'Retention and tenant-access policy'],
    constraints: ['Telemetry must not disrupt jobs', 'GPU UUID and node identity change during repair', 'High-cardinality labels are expensive'],
    answerOutline: [
      {label: 'Signal model', detail: 'Combine DCGM metrics with driver/kernel logs, scheduler state, topology, fabric/storage signals and change events; each source answers a different question.'},
      {label: 'Identity', detail: 'Maintain a small canonical mapping of site/rack/node/GPU UUID/workload/tenant and enrich outside hot metric labels where possible.'},
      {label: 'SLO views', detail: 'Begin with job success, goodput, queue time and inference SLOs; drill from customer impact to platform layer and component evidence.'},
      {label: 'Remediation', detail: 'Detect, corroborate, cordon/drain or quarantine, preserve evidence, repair, run diagnostics and only then return to service.'},
      {label: 'Reliability loop', detail: 'Use incident and repair data to identify bad cohorts, validate releases, size spares and improve pre-admission health gates.'},
    ],
    tradeoffs: [
      {decision: 'Rich labels vs cardinality', recommendation: 'Stable bounded dimensions in metrics; detailed identity in logs/traces or metadata store', alternative: 'Short-retention high-cardinality metrics only for justified drill-down use cases'},
      {decision: 'Automation vs blast radius', recommendation: 'Auto-quarantine high-confidence faults, require approval for disruptive fleet actions', alternative: 'Manual mitigation until precision and rollback are demonstrated'},
      {decision: 'Collection frequency vs overhead', recommendation: 'Tier signal cadence by operational value', alternative: 'Higher-frequency sampling only during a bounded diagnostic mode'},
    ],
    failureModes: [
      {failure: 'An alert storm quarantines an entire hardware cohort', response: 'Rate limits, blast-radius budgets, multi-signal corroboration and a circuit breaker on remediation.'},
      {failure: 'Metrics show healthy GPUs while jobs slow down', response: 'Trace beyond GPU busy state into clocks, throttling, topology, collectives, CPU/NUMA, network and storage.'},
      {failure: 'A node returns after repair with a new GPU UUID', response: 'Reconcile asset, slot and component identity; preserve historical mapping and validate inventory before admission.'},
    ],
    successMetrics: ['MTTD/MTTR for GPU-caused incidents', 'False-positive quarantine rate', 'Job failures attributed to a concrete layer', 'Telemetry overhead and cardinality growth'],
    followUps: ['The metrics backend is unavailable during an incident.', 'A firmware defect affects only one hardware revision.', 'Security forbids tenant identifiers in the central observability system.'],
    strongSignals: ['Does not treat DCGM as the whole observability system', 'Starts dashboards from workload impact', 'Makes remediation a reversible state machine', 'Controls cardinality and automation blast radius'],
  },
  {
    id: 'multi-region-ai',
    title: 'Multi-region AI platform and disaster recovery',
    category: 'Reliability & operations', difficulty: 'Staff', duration: 60,
    brief: 'A customer serves regulated inference in two regions and runs long training jobs in one. Regional GPU capacity is scarce, model artifacts are large, and data residency differs by tenant.',
    ask: 'Design failure domains, traffic, state, capacity and DR. State what is active-active, replicated, rebuilt or deliberately unavailable during a regional loss.',
    requirements: ['Inference availability 99.95%', 'Defined per-service RPO/RTO', 'Tenant data stays in approved jurisdictions', 'Regularly testable failover'],
    unknowns: ['Per-tenant residency and availability contracts', 'Model/checkpoint size and replication time', 'Regional demand correlation', 'Control-plane dependencies shared across regions'],
    constraints: ['Idle GPU standby is expensive', 'Cross-region bandwidth is bounded', 'Identical GPU SKUs are not guaranteed'],
    answerOutline: [
      {label: 'Cell model', detail: 'Make each serving region a failure-contained cell with local identity enforcement, gateway, serving, model cache, telemetry buffering and independent capacity.'},
      {label: 'State taxonomy', detail: 'Classify source data, model artifacts, configuration, secrets, indexes, checkpoints and request state by authority, residency, replication and rebuild strategy.'},
      {label: 'Traffic policy', detail: 'Route locally during health, fail only eligible tenants, bound retry amplification and define degraded models or quotas under constrained capacity.'},
      {label: 'Training recovery', detail: 'Replicate completed checkpoints asynchronously to an eligible region/object store and explicitly accept an RPO based on checkpoint cadence.'},
      {label: 'DR operations', detail: 'Automate readiness checks but gate destructive failover, prevent split brain, rehearse restore/failback and measure actual—not documented—RTO.'},
    ],
    tradeoffs: [
      {decision: 'Active-active vs warm standby', recommendation: 'Active-active inference where normal traffic uses both regions', alternative: 'Warm standby for lower tiers when cost outweighs RTO'},
      {decision: 'Uniform vs mixed GPU fleet', recommendation: 'Use model/runtime compatibility and benchmark-based capacity units', alternative: 'Uniform SKUs for the strictest predictable performance tiers'},
      {decision: 'Synchronous vs asynchronous replication', recommendation: 'Async for large model/checkpoint data with explicit RPO', alternative: 'Synchronous only for small critical metadata when latency and quorum behavior are acceptable'},
    ],
    failureModes: [
      {failure: 'Global routing sends all traffic to a region with half the capacity', response: 'Capacity-aware failover, admission control, priority tiers and pre-agreed degraded modes.'},
      {failure: 'A shared identity dependency causes both cells to fail', response: 'Cache bounded authorization state or operate a regional identity path; include shared services in failure-domain analysis.'},
      {failure: 'Failback creates divergent model/config versions', response: 'One authoritative desired state, immutable versions, reconciliation and a controlled traffic transition.'},
    ],
    successMetrics: ['Measured failover/failback RTO and data RPO', 'SLO attainment during a regional-loss exercise', 'Standby cost versus protected business impact', 'Replication lag and restore success rate'],
    followUps: ['A region is partitioned rather than fully down.', 'One jurisdiction forbids model prompts leaving the country.', 'The surviving region has a different GPU generation.'],
    strongSignals: ['Classifies state instead of saying “replicate everything”', 'Finds shared control-plane dependencies', 'Designs degraded operation for scarce capacity', 'Includes failback and regular exercises'],
  },
  {
    id: 'air-gapped-ai',
    title: 'Regulated air-gapped AI platform',
    category: 'Security & edge', difficulty: 'Staff', duration: 60,
    brief: 'A regulated customer requires an AI platform with no internet egress. Images, drivers, models and security updates cross the boundary through controlled media, and auditors require provable provenance and separation of duties.',
    ask: 'Design supply-chain, identity, network, secrets, audit and update workflows while keeping the platform operable.',
    requirements: ['No runtime internet dependency', 'Signed and approved artifacts only', 'Complete audit trail and separation of duties', 'Critical security fixes inside the mandated window'],
    unknowns: ['Regulatory control set and threat model', 'Transfer mechanism and approval latency', 'Existing PKI/HSM and identity integration', 'Offline vulnerability-feed availability'],
    constraints: ['No outbound connectivity', 'Small operations team', 'Changes occur only in approved windows'],
    answerOutline: [
      {label: 'Zones and flows', detail: 'Draw external staging, transfer gateway, quarantine/scanning, internal registries and runtime zones; define allowed one-way or mediated flows.'},
      {label: 'Artifact chain', detail: 'Mirror exact images, packages, drivers, charts and models with SBOM, malware/vulnerability scan, signatures, approvals and immutable provenance.'},
      {label: 'Runtime controls', detail: 'Local identity/PKI, workload identity, least privilege, admission verification, network default deny, encrypted storage and controlled break-glass.'},
      {label: 'Change path', detail: 'Test the complete compatible stack in a representative offline environment, promote by wave, retain known-good bundles and rehearse rollback.'},
      {label: 'Audit and response', detail: 'Write tamper-evident local audit records, export through the approved path and maintain offline incident/forensics capability.'},
    ],
    tradeoffs: [
      {decision: 'Update speed vs approval rigor', recommendation: 'Pre-approved emergency class with the same evidence but accelerated authorization', alternative: 'Normal window for non-exploitable findings'},
      {decision: 'Mirrored breadth vs attack surface', recommendation: 'Curated versioned bundles for known workloads', alternative: 'Broader internal mirror if developer autonomy is a contractual requirement'},
      {decision: 'Central vs local identity', recommendation: 'Local survivable identity anchored to enterprise governance', alternative: 'Fully isolated identity if cross-domain trust is prohibited'},
    ],
    failureModes: [
      {failure: 'A critical CVE arrives between change windows', response: 'Risk triage, emergency approval path, prebuilt test evidence, compensating controls and time-bounded exception.'},
      {failure: 'An artifact signature is valid but its source is unapproved', response: 'Verify signer, provenance, digest, policy and promotion record—not signature validity alone.'},
      {failure: 'Internal certificate revocation cannot reach external services', response: 'Operate local trust and revocation distribution with expiry monitoring and rehearsed rotation.'},
    ],
    successMetrics: ['Patch latency by severity', 'Unapproved artifact admission count: zero', 'Bundle promotion success and rollback time', 'Audit evidence completeness and control-test pass rate'],
    followUps: ['The site remains disconnected for one year.', 'A vendor hotfix is available only as an unsigned binary.', 'Operations needs remote vendor support during an outage.'],
    strongSignals: ['Draws the artifact path as carefully as the runtime path', 'Treats provenance as more than signatures', 'Balances patch urgency with controlled evidence', 'Designs offline day-2 operations'],
  },
  {
    id: 'fleet-upgrade',
    title: 'Fleet-wide firmware, driver and OS upgrade',
    category: 'Reliability & operations', difficulty: 'Senior', duration: 45,
    brief: 'Hundreds of GPU nodes span four hardware generations. Security requires a kernel update, the new driver has a compatibility dependency, and production demand leaves little spare capacity.',
    ask: 'Design inventory, compatibility, canary, rollout, rollback and customer communication for the coordinated change.',
    requirements: ['No all-at-once outage', 'Security deadline is met', 'Performance regressions stop automatically', 'Every node converges to an approved state'],
    unknowns: ['Exact hardware/firmware/driver/runtime matrix', 'Spare capacity and workload drain behavior', 'Rollback support across firmware and data formats', 'Representative canary workloads'],
    constraints: ['Heterogeneous fleet', 'Long-running jobs', 'Some firmware changes are not safely reversible'],
    answerOutline: [
      {label: 'Inventory', detail: 'Build cohort keys from hardware, BMC/BIOS, NIC/DPU, GPU firmware, kernel, driver, container runtime and workload dependencies; block unknown states.'},
      {label: 'Compatibility contract', detail: 'Define a tested bundle and supported skew window across host and containers; validate upgrade and rollback paths before production.'},
      {label: 'Canary', detail: 'Sample every material cohort and run diagnostics plus representative training, inference, fabric and storage benchmarks.'},
      {label: 'Waves', detail: 'Cordon, checkpoint/drain, update, reboot, validate, observe and admit; rate-limit each failure domain and stop on health/SLO guardrails.'},
      {label: 'Recovery', detail: 'Separate reversible software rollback from firmware recovery plans; keep known-good images, spares and explicit escalation boundaries.'},
    ],
    tradeoffs: [
      {decision: 'Security speed vs availability', recommendation: 'Risk-based waves with bounded concurrent capacity loss', alternative: 'Emergency maintenance when active exploitation exceeds availability risk'},
      {decision: 'In-place update vs reprovision', recommendation: 'Immutable reprovision where practical for repeatability', alternative: 'In-place firmware steps that require hardware-specific tooling'},
      {decision: 'Broad vs narrow canary', recommendation: 'Minimum one representative of every material cohort', alternative: 'Larger statistical canary for noisy performance baselines'},
    ],
    failureModes: [
      {failure: 'Canary succeeds but one older NIC cohort regresses', response: 'Cohort-aware canaries and rollout guardrails; stop that cohort without blocking independently validated ones.'},
      {failure: 'Jobs cannot drain before the security deadline', response: 'Checkpoint/preemption policy, owner escalation, capacity reservation and an explicit risk decision—not silent deadline drift.'},
      {failure: 'Firmware rollback is unsupported', response: 'Use spare/replace strategy, lab proof, vendor recovery procedure and smaller initial blast radius.'},
    ],
    successMetrics: ['Convergence and drift by cohort', 'Upgrade failure/rollback rate', 'Capacity unavailable during each wave', 'Pre/post performance and workload SLO delta'],
    followUps: ['The first wave shows a 4% training regression.', 'A zero-day cuts the deadline to 48 hours.', 'Twenty percent of nodes have unknown firmware inventory.'],
    strongSignals: ['Models the change as a compatibility bundle', 'Makes canaries representative by cohort', 'Distinguishes halt from rollback', 'Protects workload goodput during maintenance'],
  },
  {
    id: 'edge-inference',
    title: 'Disconnected edge inference fleet',
    category: 'Security & edge', difficulty: 'Senior', duration: 45,
    brief: 'Hundreds of remote sites run GPU inference with intermittent low-bandwidth connectivity, minimal local staff and variable power quality. Decisions must continue locally during WAN outages.',
    ask: 'Design the site cell, model rollout, telemetry, security and remote recovery model.',
    requirements: ['Local inference during 72-hour disconnection', 'Fleet-wide version visibility', 'Safe staged model updates and rollback', 'No sensitive raw data leaves the site'],
    unknowns: ['Site compute/power envelope', 'Connectivity distribution and transfer cost', 'Local data retention and privacy rules', 'Acceptable model staleness'],
    constraints: ['Minimal hands-on support', 'Intermittent WAN', 'Hardware cannot be identical at every site'],
    answerOutline: [
      {label: 'Autonomous site cell', detail: 'Local gateway, policy, model store, inference runtime, durable work queue, telemetry buffer and health supervisor operate without the central plane.'},
      {label: 'Desired state', detail: 'Central control publishes signed versioned manifests; sites reconcile when connected and report observed state without requiring constant reachability.'},
      {label: 'Rollout', detail: 'Use rings by hardware/connectivity cohort, resumable downloads, checksum/signature verification, A/B slots and local automatic rollback.'},
      {label: 'Observability', detail: 'Aggregate and prioritize store-and-forward telemetry; retain detailed evidence locally and upload within bandwidth/privacy budgets.'},
      {label: 'Recovery', detail: 'Watchdog and local health checks restart bounded components; secure remote access is audited, time-limited and not required for basic recovery.'},
    ],
    tradeoffs: [
      {decision: 'Central control vs local autonomy', recommendation: 'Central intent with locally enforced safe policy', alternative: 'Central synchronous control only for sites with a contractual connectivity SLA'},
      {decision: 'Full vs delta model updates', recommendation: 'Resumable content-addressed chunks where complexity is justified', alternative: 'Full immutable bundles for smaller models and simpler recovery'},
      {decision: 'Telemetry detail vs bandwidth', recommendation: 'Local high-detail ring buffer plus central aggregates/events', alternative: 'Burst upload detailed evidence on incident demand'},
    ],
    failureModes: [
      {failure: 'Power loss interrupts a model update', response: 'Write to inactive slot, verify fully, then atomically switch; boot the last known-good version.'},
      {failure: 'A bad model reaches sites that remain offline for weeks', response: 'Expiry and revocation policy, ring rollout, local safety guardrails and rollback intent delivered on reconnection.'},
      {failure: 'Telemetry backlog consumes local disk', response: 'Priority queues, bounded retention, compaction and deletion policy that never blocks inference.'},
    ],
    successMetrics: ['Inference availability during WAN loss', 'Fleet version convergence and rollback time', 'Data transferred per site', 'Remote recovery success and truck-roll rate'],
    followUps: ['A site is unreachable for 90 days.', 'Model size doubles but bandwidth does not.', 'Attackers gain physical access to one appliance.'],
    strongSignals: ['Makes each site independently safe', 'Uses desired/observed state reconciliation', 'Designs atomic update and rollback', 'Treats bandwidth and physical security as first-class'],
  },
  {
    id: 'gpu-estate-tco',
    title: 'GPU estate TCO and capacity redesign',
    category: 'Migration & economics', difficulty: 'Senior', duration: 45,
    brief: 'Executives see 35% average GPU activity and want to cancel future purchases. Researchers argue that scheduled capacity, memory footprint and topology—not average activity—determine whether work can run.',
    ask: 'Build an evidence-based capacity and commercial recommendation without confusing allocation, activity and useful work.',
    requirements: ['Quantify current demand and waste', 'Protect critical workload SLOs', 'Create showback/chargeback incentives', 'Produce a 12–24 month capacity plan'],
    unknowns: ['Queue-time and completion SLOs', 'Workload shape and seasonality', 'Contract/reservation commitments', 'Business value and priority per workload'],
    constraints: ['Political sensitivity between teams', 'Procurement lead times', 'Telemetry quality is inconsistent'],
    answerOutline: [
      {label: 'Metric contract', detail: 'Separate available, allocated, GPU-active and useful successful GPU-hours; combine scheduler, DCGM, job outcome and business/service class.'},
      {label: 'Demand model', detail: 'Segment by GPU type/count/memory/topology, duration, priority, deadline and checkpointability; model peaks and fragmentation, not only averages.'},
      {label: 'Interventions', detail: 'Right-size requests, reclaim abandoned allocations, queue low-priority work, use compatible sharing, consolidate pools and improve failure/goodput before buying.'},
      {label: 'Capacity plan', detail: 'Forecast base, peak and contingency demand with lead time, spare/failure allowance and explicit SLO/cost scenarios.'},
      {label: 'Governance', detail: 'Publish transparent allocation policy and showback first; phase chargeback with exception and dispute mechanisms.'},
    ],
    tradeoffs: [
      {decision: 'Utilization vs queue time', recommendation: 'Optimize goodput subject to service-class queue SLOs', alternative: 'Higher reservations for deadline-critical work'},
      {decision: 'Owned vs elastic capacity', recommendation: 'Own predictable base demand and burst where data/security/availability allow', alternative: 'More owned headroom when external capacity is unreliable'},
      {decision: 'Showback vs chargeback', recommendation: 'Start with trusted showback and behavioral feedback', alternative: 'Chargeback once measurement and allocation policy are accepted'},
    ],
    failureModes: [
      {failure: 'Average utilization rises but job completion worsens', response: 'Track goodput, failures, preemption loss and queue time; utilization is a constraint signal, not the business outcome.'},
      {failure: 'Fractional sharing causes memory or latency interference', response: 'Benchmark by workload class and revert incompatible workloads to full or isolated GPUs.'},
      {failure: 'Demand forecast misses a launch peak', response: 'Scenario bands, reservations, admission tiers, elastic option and a documented degraded-capacity plan.'},
    ],
    successMetrics: ['Goodput per available GPU-hour', 'Queue SLO by priority and shape', 'Cost per successful workload outcome', 'Forecast error and stranded-capacity rate'],
    followUps: ['The CFO mandates a 25% run-rate reduction.', 'Cloud burst is prohibited by data policy.', 'A new model needs 8-GPU topology-aligned nodes.'],
    strongSignals: ['Rejects average activity as a sufficient metric', 'Includes fragmentation and failure waste', 'Connects capacity to workload SLOs', 'Makes commercial options explicit'],
  },
  {
    id: 'platform-consolidation',
    title: 'Post-acquisition GPU platform consolidation',
    category: 'Migration & economics', difficulty: 'Staff', duration: 60,
    brief: 'Two companies merge: one operates Slurm on bare metal, the other Kubernetes in cloud and on-prem. Identity, network, registries, observability and vendor contracts differ, but leadership demands one operating model in 12 months.',
    ask: 'Design the target-state decision process and a migration that reduces risk while delivering visible early value.',
    requirements: ['No disruption to revenue or research deadlines', 'Unified access and governance', 'Consolidated cost and reliability reporting', 'Clear target platform and ownership'],
    unknowns: ['Workload inventory and immovable dependencies', 'Contract exit dates and hardware lifecycle', 'Data residency/network overlap', 'Team skills and political constraints'],
    constraints: ['Leadership deadline', 'Overlapping network/identity namespaces', 'Both current platforms must operate during transition'],
    answerOutline: [
      {label: 'Discover', detail: 'Inventory workloads, data, SLOs, hardware, integrations, contracts, skills and failure history; classify retain, migrate, retire or re-platform.'},
      {label: 'Choose principles', detail: 'Define workload-placement rules, trust boundaries, supported golden paths and exceptions before selecting a single product label.'},
      {label: 'Build landing zones', detail: 'Unify identity federation, artifact provenance, network connectivity, telemetry vocabulary and cost taxonomy as migration enablers.'},
      {label: 'Migrate in waves', detail: 'Start with low-coupling workloads, dual-run where validation needs it, compare outcomes, define rollback and progressively close old entry points.'},
      {label: 'Decommission', detail: 'Require owner/data/dependency confirmation, archive audit evidence, terminate contracts and measure eliminated cost and toil.'},
    ],
    tradeoffs: [
      {decision: 'One platform vs bounded coexistence', recommendation: 'One governance model with workload-fit execution paths', alternative: 'Force one scheduler only when it demonstrably covers both workload classes'},
      {decision: 'Lift-and-shift vs re-platform', recommendation: 'Re-platform high-value long-lived workloads; contain temporary lift-and-shift debt', alternative: 'Retain immovable workloads until lifecycle retirement'},
      {decision: 'Fast standardization vs team adoption', recommendation: 'Time-boxed standards with joint design and measured migration waves', alternative: 'Executive exception process for deadline-critical systems'},
    ],
    failureModes: [
      {failure: 'Identity merge accidentally broadens data access', response: 'Default deny, entitlement mapping tests, staged federation and auditable access reconciliation.'},
      {failure: 'Dual-run doubles cost without proving readiness', response: 'Every parallel period has validation criteria, owner, budget and exit date.'},
      {failure: 'Target platform lacks a critical batch capability', response: 'Use workload-fit acceptance tests before commitment; retain a bounded path rather than hiding the gap.'},
    ],
    successMetrics: ['Workloads migrated/retired with SLO maintained', 'Duplicate platform cost and toil removed', 'Access-policy reconciliation defects', 'Lead time on the supported golden paths'],
    followUps: ['The acquired team will lose half its operators.', 'One contract cannot exit for three years.', 'Leadership insists Kubernetes must run every workload.'],
    strongSignals: ['Discovers before standardizing', 'Separates governance unification from scheduler uniformity', 'Defines wave exit criteria', 'Treats skills and contracts as architecture constraints'],
  },
  {
    id: 'global-cloud-saas',
    title: 'Multi-region cloud SaaS platform',
    category: 'Cloud & Kubernetes', difficulty: 'Staff', duration: 60,
    brief: 'A B2B SaaS product serves European and US customers. It currently runs in one cloud region, handles 8,000 requests/second at peak, stores tenant data in PostgreSQL and object storage, and has suffered two region-level outages. Enterprise contracts now require 99.95% availability and regional data residency.',
    ask: 'Design the target cloud architecture from DNS and edge through compute, data, delivery, security and operations. State exactly which failures it survives and how.',
    requirements: ['99.95% user-facing availability', 'EU and US tenant data remains in-region', 'RPO <= 5 minutes and RTO <= 30 minutes for the premium tier', 'Zero-downtime application delivery and auditable access'],
    unknowns: ['Read/write patterns and tenant placement model', 'Session state and asynchronous workflow semantics', 'Traffic growth, peak correlation and cost ceiling', 'Whether cross-region failover is legally allowed per tenant'],
    constraints: ['Existing single-region PostgreSQL schema', 'Small platform team and limited 24x7 database expertise', 'Migration cannot pause customer writes', 'Cloud-provider managed services are acceptable but lock-in must be explicit'],
    answerOutline: [
      {label: 'Cell and tenancy model', detail: 'Partition tenants into independently deployable regional cells. Each cell owns ingress, stateless services, queues, cache and regional data; a global control plane maps tenant to cell but stays off the synchronous request path.'},
      {label: 'Request path', detail: 'Authoritative DNS and edge protection route to a regional load balancer, private application tier and local dependencies. Bound timeouts/retries and keep sessions stateless or region-local.'},
      {label: 'Data strategy', detail: 'Keep tenant source-of-truth in the approved region, use multi-AZ HA locally, stream encrypted backups/change data to an eligible DR location, and classify every dataset by RPO, RTO and rebuildability.'},
      {label: 'Failure behavior', detail: 'Automate zone failure inside a region; gate region failover by residency, capacity and data freshness. Define degraded read-only or queued-write modes when safe rather than promising universal active-active writes.'},
      {label: 'Migration and proof', detail: 'Introduce tenant routing, build the second cell, replicate and shadow traffic, migrate cohorts with reconciliation, then run zone/region and restore exercises against measured SLOs.'},
    ],
    tradeoffs: [
      {decision: 'Active-active vs active-passive', recommendation: 'Active-active regional cells for traffic, with single-writer ownership per tenant', alternative: 'Multi-writer only when conflict semantics and consistency requirements justify its operational cost'},
      {decision: 'Managed database vs self-managed', recommendation: 'Managed multi-AZ database for the small team', alternative: 'Self-managed only for requirements the managed service cannot meet and with funded operational ownership'},
      {decision: 'Shared vs cell-local platform services', recommendation: 'Keep synchronous dependencies cell-local', alternative: 'Share only control/reporting systems whose outage cannot break serving'},
    ],
    failureModes: [
      {failure: 'Global control plane is unavailable', response: 'Existing tenant-to-cell routes remain cached and regional cells serve independently; new tenant placement waits.'},
      {failure: 'One region is lost with replication lag', response: 'Fail only eligible tenants, enforce the declared RPO, reconcile queued/idempotent writes and communicate actual data freshness.'},
      {failure: 'Failover doubles load on the surviving region', response: 'Capacity reserve, admission tiers, load shedding and a tested degraded mode prevent retry-driven collapse.'},
    ],
    successMetrics: ['Availability and latency SLO by region/tier', 'Measured zone/region failover RTO and replication RPO', 'Cost per tenant/request and reserved headroom', 'Restore/failback success and reconciliation defects'],
    followUps: ['The US region is healthy but the global DNS provider fails.', 'EU law now forbids even encrypted backups outside the EU.', 'Traffic grows 5x during a launch while the secondary region is degraded.'],
    strongSignals: ['Uses cells and removes global dependencies from the request path', 'Classifies state instead of saying “replicate everything”', 'Defines degraded behavior and capacity during failover', 'Includes migration, failback and game-day proof'],
  },
  {
    id: 'production-kubernetes-platform',
    title: 'Production multi-cluster Kubernetes platform',
    category: 'Cloud & Kubernetes', difficulty: 'Staff', duration: 60,
    brief: 'Seventy product teams run 450 services across development and production. Clusters evolved independently, upgrades are delayed, noisy tenants cause incidents, and every team reinvents ingress, secrets, observability and deployment patterns.',
    ask: 'Design a multi-cluster Kubernetes platform and operating model that improves reliability and developer speed without building an unmaintainable internal cloud.',
    requirements: ['Failure isolation between environments and critical business domains', 'Supported golden paths for HTTP services, workers and stateful dependencies', 'Quarterly Kubernetes upgrades with controlled workload disruption', 'Self-service with policy, cost and ownership visibility'],
    unknowns: ['Team trust boundaries and compliance domains', 'Workload resource/topology/state characteristics', 'Current CNI, ingress, identity and GitOps choices', 'Platform-team staffing and support contract'],
    constraints: ['Existing workloads cannot all migrate at once', 'Some teams need privileged or specialized nodes', 'Cloud quota and regional capacity vary', 'Platform must support—not own—application reliability'],
    answerOutline: [
      {label: 'Cluster strategy', detail: 'Choose cluster boundaries from blast radius, trust, lifecycle and capacity—not one cluster per team. Separate production from non-production and use bounded domain/region cells for critical workloads.'},
      {label: 'Platform contract', detail: 'Offer versioned service templates covering namespace, workload identity, network policy, resource/probe/PDB defaults, ingress, secrets, telemetry, rollout and ownership metadata.'},
      {label: 'Control layers', detail: 'Central fleet inventory and policy publish intent; cluster-local control planes and add-ons keep serving. GitOps reconciles auditable desired state with explicit field ownership and exception lifecycle.'},
      {label: 'Capacity and reliability', detail: 'Model requests, topology and disruption; isolate special node pools, enforce quotas/priorities, preserve system reserve, and test autoscaler behavior at cloud quotas and failure-domain limits.'},
      {label: 'Lifecycle and adoption', detail: 'Qualify version/add-on bundles, canary representative clusters and workloads, roll by rings, publish deprecations, measure adoption and fund migration help.'},
    ],
    tradeoffs: [
      {decision: 'Few large vs many small clusters', recommendation: 'Bounded domain/region clusters sized for team capacity', alternative: 'Dedicated clusters for hard compliance, lifecycle or blast-radius requirements'},
      {decision: 'Central platform API vs raw Kubernetes', recommendation: 'Golden paths with escape hatches and transparent Kubernetes underneath', alternative: 'Higher abstraction only when the team can version and support the contract long-term'},
      {decision: 'Global vs cluster-local add-ons', recommendation: 'Cluster-local data-plane dependencies with centrally managed versions', alternative: 'Shared services only where their outage and tenancy model are acceptable'},
    ],
    failureModes: [
      {failure: 'A broken admission policy blocks all deployments', response: 'Canary policies, failure-policy choice per risk, break-glass with audit and independent control-plane SLOs.'},
      {failure: 'Autoscaler cannot add nodes during peak', response: 'Quota/capacity telemetry, reserved headroom, priority/preemption policy and bounded workload admission.'},
      {failure: 'Upgrade drains violate application availability', response: 'Preflight PDB/topology/capacity checks, workload owner gates, surge capacity and rollback or pause by cluster ring.'},
    ],
    successMetrics: ['Platform and workload SLO attainment', 'Lead time from repo to production', 'Upgrade currency and exception age', 'Policy violations, toil, cost efficiency and golden-path adoption'],
    followUps: ['A regulated team requires host-level isolation.', 'One region cannot supply the required instance family.', 'A team demands a six-month Kubernetes-version exception.'],
    strongSignals: ['Derives cluster boundaries from failure and trust domains', 'Defines a supported product contract and ownership boundary', 'Treats upgrades/capacity as continuous operations', 'Measures developer outcomes and reliability together'],
  },
  {
    id: 'secure-cicd-supply-chain',
    title: 'Secure enterprise CI/CD supply chain',
    category: 'CI/CD & platform engineering', difficulty: 'Staff', duration: 60,
    brief: 'A company runs GitHub Actions and self-hosted runners for 600 repositories. Builds can access production credentials, artifacts are not consistently signed, pipeline duration is rising, and an audit found that a compromised pull request could reach a privileged runner.',
    ask: 'Design the end-to-end software delivery system from commit through production verification, including trust boundaries, artifact provenance, approvals, rollback and platform operations.',
    requirements: ['Untrusted pull requests cannot access trusted networks or credentials', 'Every production artifact is immutable, traceable and verified', 'Median pipeline under 12 minutes with protected emergency delivery', 'Progressive deployment with automated SLO rollback'],
    unknowns: ['Repository languages/build shapes and test cost', 'Regulatory approval and separation-of-duty rules', 'Target environments and deployment frequencies', 'Current secret, PKI, registry and policy systems'],
    constraints: ['Self-hosted runners are required for private dependencies', 'No long-lived cloud credentials', 'Hundreds of legacy pipelines need gradual migration', 'Emergency changes must remain possible and auditable'],
    answerOutline: [
      {label: 'Trust zones', detail: 'Separate ephemeral untrusted PR runners from protected branch/release runners by identity, network and cache. Never promote an execution environment merely because workflow YAML asks.'},
      {label: 'Build once', detail: 'Compile/test/scan once, create SBOM and provenance, sign the immutable digest, store it in a protected registry and promote the same digest across environments.'},
      {label: 'Authorization', detail: 'Use short-lived workload identity with repository/ref/environment claims, policy-bound permissions, protected environments and independent approval for high-risk production actions.'},
      {label: 'Deployment path', detail: 'Declarative release references the verified digest; canary or blue/green rollout evaluates availability, latency, correctness and business guardrails before expansion.'},
      {label: 'Platform operations', detail: 'Treat runners, caches, registries, policy and deployment controllers as SLO-backed services with capacity, patching, isolation, audit and disaster recovery.'},
    ],
    tradeoffs: [
      {decision: 'Hosted vs self-hosted runners', recommendation: 'Hosted/isolated ephemeral runners for untrusted work; dedicated ephemeral self-hosted pools for private access', alternative: 'Persistent runners only with strong isolation and accepted residue risk'},
      {decision: 'Rebuild vs promote', recommendation: 'Promote one signed artifact digest', alternative: 'Rebuild only where environment-specific compilation is unavoidable and provenance remains independently verifiable'},
      {decision: 'Automatic vs manual production approval', recommendation: 'Risk-based policy: automated low-risk promotion, human separation for material/high-risk changes', alternative: 'Manual gates where regulation explicitly requires them'},
    ],
    failureModes: [
      {failure: 'Malicious PR poisons a shared build cache', response: 'Trust-scoped cache keys/stores, read-only or no trusted cache for untrusted jobs, artifact verification and ephemeral cleanup.'},
      {failure: 'Signing service is unavailable', response: 'Fail closed for new production promotion, keep current production serving and use a narrowly audited emergency process.'},
      {failure: 'Canary metrics are missing', response: 'Classify evidence as unknown and hold; never interpret missing telemetry as a successful rollout.'},
    ],
    successMetrics: ['Commit-to-production lead time and queue time', 'Change failure/rollback and SLO regression rate', 'Verified-artifact coverage and policy exceptions', 'Runner utilization, isolation defects and credential lifetime'],
    followUps: ['The signing key is suspected compromised.', 'A zero-day requires deployment within two hours.', 'A monorepo now produces 200 independently deployable services.'],
    strongSignals: ['Draws explicit trusted and untrusted execution zones', 'Builds once and promotes immutable identity', 'Uses workload identity rather than static secrets', 'Treats missing rollout evidence as unknown'],
  },
  {
    id: 'gitops-promotion-platform',
    title: 'GitOps promotion and rollback platform',
    category: 'CI/CD & platform engineering', difficulty: 'Senior', duration: 45,
    brief: 'Teams use GitOps, but production promotion means copying YAML between branches, emergency fixes drift from Git, controller ownership conflicts with admission mutation, and rollback procedures vary by team.',
    ask: 'Design repositories, promotion, reconciliation, secrets, policy, rollback and break-glass for a multi-environment GitOps platform.',
    requirements: ['Auditable promotion of the same artifact', 'Environment-specific configuration without copy/paste drift', 'Safe reconciliation and rollback', 'Break-glass that does not permanently bypass Git'],
    unknowns: ['Number of clusters/tenants and team ownership', 'Artifact versus configuration release cadence', 'Secret-management system', 'Required approval and policy model'],
    constraints: ['Git remains the desired-state authority', 'Some incidents need immediate runtime action', 'Multiple controllers and mutating webhooks already exist'],
    answerOutline: [
      {label: 'Repository contract', detail: 'Separate application source/build from environment desired state; reference immutable artifact digests and use overlays or generated config with clear ownership rather than branch copying.'},
      {label: 'Promotion', detail: 'Automation opens a reviewed environment change for the same digest, includes evidence/policy results, and records who approved what; no environment rebuild.'},
      {label: 'Reconciliation', detail: 'Scope controller tenancy, dependencies and sync waves; use health checks, timeouts, retry limits and deterministic admission mutation.'},
      {label: 'Secrets and policy', detail: 'Commit encrypted references—not plaintext; reconcile from an external secret authority and validate admission/security policy before merge and at cluster boundary.'},
      {label: 'Rollback and break-glass', detail: 'Revert desired state to a compatible digest/config; emergency runtime changes require time-bound access, audit and an automatic reconciliation PR before normal control resumes.'},
    ],
    tradeoffs: [
      {decision: 'Mono-repo vs repository per team/environment', recommendation: 'Choose boundaries that match ownership and blast radius; standardize contract rather than one topology', alternative: 'Separate repositories for hard access/compliance boundaries'},
      {decision: 'Automatic sync vs approval', recommendation: 'Automatic after reviewed desired-state merge for normal changes', alternative: 'Manual sync for bounded high-risk transitions, never as permanent process debt'},
      {decision: 'Ignore differences vs field ownership', recommendation: 'Fix deterministic ownership first', alternative: 'Ignore only exact non-semantic fields with documented risk and tests'},
    ],
    failureModes: [
      {failure: 'Bad commit affects every cluster', response: 'Repository/tenant boundaries, cluster rings, staged reconciliation and automatic halt prevent global fan-out.'},
      {failure: 'Controller deletes manually created emergency resource', response: 'Use a documented suspension/ownership path and immediately capture the emergency state as reviewed desired state.'},
      {failure: 'Rollback binary is incompatible with migrated schema', response: 'Compatibility metadata and expand/contract delivery make rollback a system decision, not merely git revert.'},
    ],
    successMetrics: ['Desired-to-observed convergence and drift age', 'Promotion lead time and rollback success', 'Break-glass frequency/time-to-reconcile', 'Controller errors, API pressure and policy exception age'],
    followUps: ['Git hosting is unavailable during an incident.', 'A webhook injects a timestamp on every apply.', 'One cluster must remain two releases behind.'],
    strongSignals: ['Separates artifact build from environment promotion', 'Defines controller and field ownership', 'Makes break-glass converge back through Git', 'Checks data/schema compatibility before rollback'],
  },
  {
    id: 'internal-developer-platform',
    title: 'Internal developer platform for 100 teams',
    category: 'CI/CD & platform engineering', difficulty: 'Staff', duration: 60,
    brief: 'Developers need weeks to provision a production service because they coordinate tickets across cloud, Kubernetes, DNS, secrets, observability and security teams. Leadership asks for a self-service platform but does not want a large custom portal that hides failures.',
    ask: 'Design the platform product, APIs, golden paths, ownership model, adoption plan and reliability measures.',
    requirements: ['Production-ready service in under one hour', 'Security and reliability defaults with controlled exceptions', 'Works across two clouds and Kubernetes', 'Teams retain application ownership and can understand generated infrastructure'],
    unknowns: ['Top developer journeys and current ticket wait time', 'Team maturity and required escape hatches', 'Platform staffing/support budget', 'Which cloud differences genuinely matter to consumers'],
    constraints: ['Cannot migrate every workload', 'Existing Terraform/GitOps investments must be reused', 'Portal UI is optional; stable contracts are mandatory', 'Platform team must not become approval bottleneck'],
    answerOutline: [
      {label: 'Product discovery', detail: 'Measure the highest-friction journeys and define consumers, outcomes and non-goals. Start with one paved road such as stateless HTTP service—not a universal abstraction.'},
      {label: 'Platform API', detail: 'Expose versioned service/resource contracts through templates or declarative APIs; implementation composes Terraform, GitOps, identity, DNS, secrets and observability visibly.'},
      {label: 'Golden path', detail: 'A new service includes repository/pipeline, runtime, identity, network, secrets, SLO dashboard, alerts, ownership, cost tags and rollback from the first deployment.'},
      {label: 'Guardrails and exceptions', detail: 'Policy provides safe defaults and fast feedback. Exceptions have owner, rationale, compensating control and expiry—not permanent forks.'},
      {label: 'Adoption and operations', detail: 'Pilot with willing teams, measure time/toil/reliability, document support boundaries, version/deprecate contracts and run the platform itself against SLOs.'},
    ],
    tradeoffs: [
      {decision: 'Portal vs APIs/templates', recommendation: 'Contract-first APIs/templates; add portal as discoverability/workflow UI', alternative: 'Portal-first only when it does not become the only debugging/control surface'},
      {decision: 'Cloud abstraction vs native capability', recommendation: 'Abstract common intent and expose deliberate provider-specific extensions', alternative: 'Lowest-common-denominator only for truly portable workloads'},
      {decision: 'Mandatory vs opt-in adoption', recommendation: 'Make the paved road measurably easier, then mandate only critical controls', alternative: 'Forced migration for expired/unsafe platforms with funded assistance'},
    ],
    failureModes: [
      {failure: 'Platform API outage blocks deployments', response: 'Existing workloads keep serving; queued/idempotent workflows resume, and documented lower-level recovery remains available.'},
      {failure: 'Template creates insecure resources', response: 'Versioned templates, policy tests, canary tenants, blast-radius controls and rapid deprecation/upgrade path.'},
      {failure: 'Teams fork the golden path', response: 'Measure reasons, fix missing capabilities, keep extension points and make unsupported ownership explicit.'},
    ],
    successMetrics: ['Time to first production deployment and developer wait time', 'Golden-path adoption/retention and exception age', 'Change failure rate and platform SLO', 'Ticket/toil reduction and cost attribution coverage'],
    followUps: ['Half the teams do not use Kubernetes.', 'The platform team has only four engineers.', 'A cloud-native database feature cannot be expressed in the common API.'],
    strongSignals: ['Starts from developer journeys and outcomes', 'Treats the platform as a versioned product/API', 'Includes ownership and escape hatches', 'Avoids lowest-common-denominator multi-cloud abstraction'],
  },
  {
    id: 'observability-slo-platform',
    title: 'Multi-tenant observability and SLO platform',
    category: 'Cloud & Kubernetes', difficulty: 'Staff', duration: 60,
    brief: 'Hundreds of services send metrics, logs and traces to separate stacks. Costs grow 20% monthly, cardinality incidents make alerting unavailable, and teams have dashboards but cannot answer whether customers are affected.',
    ask: 'Design telemetry collection, tenancy, storage, SLOs, alerting, cost controls and operational ownership for a shared observability platform.',
    requirements: ['Critical alert evaluation survives regional/backend failure', 'Tenant isolation and predictable cost', 'Consistent correlation across metrics/logs/traces', 'Service teams can define actionable SLOs and debug incidents'],
    unknowns: ['Telemetry volume/cardinality and query patterns', 'Retention/compliance by signal and tenant', 'Existing OpenTelemetry/Prometheus adoption', 'Which alerts require local survivability'],
    constraints: ['Cannot centralize every high-cardinality event as a metric', 'Telemetry loss must be explicit rather than reported healthy', 'Migration must not break paging', 'Budget requires tiered retention'],
    answerOutline: [
      {label: 'Signal contracts', detail: 'Standardize service, environment, region, version, owner and correlation identity; define allowed metric labels and route high-cardinality detail to logs/traces.'},
      {label: 'Collection path', detail: 'Use node/cluster-local collectors with batching, memory/disk queues, sampling/redaction and independent health; decouple applications from backend endpoints.'},
      {label: 'Storage and tenancy', detail: 'Separate ingestion/query limits, retention and access by tenant/signal tier; protect critical recording/alert rules from exploratory workload.'},
      {label: 'SLO layer', detail: 'Build SLIs from user journeys, define error budgets and multi-window burn alerts; connect alerts to ownership and runbooks rather than dashboard thresholds.'},
      {label: 'Operate and govern', detail: 'SLO the telemetry pipeline, expose dropped/late/unknown data, attribute cost, review cardinality and provide instrumentation lifecycle/deprecation.'},
    ],
    tradeoffs: [
      {decision: 'Central vs federated alerting', recommendation: 'Local critical evaluation with central long-term aggregation and governance', alternative: 'Fully central only when dependency failure meets paging survivability requirements'},
      {decision: 'Tail vs probabilistic trace sampling', recommendation: 'Head sampling baseline plus bounded tail rules for errors/latency', alternative: 'Higher sampling for critical low-volume paths'},
      {decision: 'Retention vs cost', recommendation: 'Tier by operational/compliance value and downsample aggregates', alternative: 'Long raw retention only for justified investigations or regulation'},
    ],
    failureModes: [
      {failure: 'Remote-write backend is unavailable', response: 'Collectors buffer within bounded disk/memory, local alerts continue, drops are explicit and replay does not overload recovery.'},
      {failure: 'One tenant creates unbounded labels', response: 'Per-tenant limits, ingestion guardrails, quarantine and cost visibility protect shared alerting/query capacity.'},
      {failure: 'Trace sampling omits the incident', response: 'Always retain errors/critical paths within caps, correlate with logs/metrics and describe observability as probabilistic evidence.'},
    ],
    successMetrics: ['Alert evaluation availability and notification latency', 'Telemetry completeness/drop/lag by tenant', 'Cardinality and cost per service/signal', 'SLO coverage, actionable page rate and incident MTTR'],
    followUps: ['The central backend is unavailable for six hours.', 'A tenant doubles trace volume overnight.', 'Security forbids raw customer identifiers in the shared platform.'],
    strongSignals: ['Treats the observability system as an SLO-backed production service', 'Separates stable metric dimensions from high-cardinality detail', 'Makes missing telemetry unknown', 'Protects critical alerting from exploratory load'],
  },
  {
    id: 'multi-region-api-platform',
    title: 'Multi-region highly-available API platform',
    category: 'Cloud & Kubernetes', difficulty: 'Staff', duration: 60,
    brief: 'A company\'s core public API serves millions of requests per day from a single primary region with a warm secondary. A six-hour regional outage last year showed customers noticing within minutes, while a human decision was still needed to promote the secondary for writes. Leadership wants the platform to survive a full regional cloud outage with minimal customer-visible impact.',
    ask: 'Design the DNS/traffic layer, compute topology, cross-region data-consistency model and failover mechanics. State the RTO/RPO the design actually achieves and why.',
    requirements: ['Automatic detection and failover for read traffic without waiting on a human decision', 'Explicit, measured RTO and RPO commitments for writes', 'No silent data loss or dual writers during failover', 'Failover and failback are regularly tested, not tabletop-only'],
    unknowns: ['Read/write ratio and which endpoints can safely serve stale data', 'Client and resolver DNS-cache behavior in the real client population', 'Acceptable write-unavailability window during a hard failover', 'Whether any tenants are contractually pinned to a single region'],
    constraints: ['Existing primary-region relational database with synchronous local replicas only', 'DNS-based failover has a floor propagation time regardless of configured TTL', 'Cost ceiling rules out full active-active writes for every dataset', 'Some enterprise customers contractually require single-region data residency'],
    answerOutline: [
      {label: 'Traffic layer', detail: 'Front both regions with a global anycast or multi-region load balancer where the provider supports it, and treat DNS TTL as a floor rather than a guarantee, since resolver and client caches routinely ignore it; pair it with connection-level retry/redirect for clients that do respect DNS.'},
      {label: 'Active-active reads, single-writer per shard', detail: 'Serve reads active-active from a local replica in each region, but keep one writer region per tenant or shard so there is always a single arbiter of truth; reserve true multi-writer active-active for the narrow set of data whose domain can tolerate conflict resolution.'},
      {label: 'Consistency by data class', detail: 'Classify data by what staleness actually costs: session and cache state tolerates seconds of lag, balances or inventory need synchronous or quorum writes, and everything else gets an explicit, monitored lag budget rather than an assumed guarantee.'},
      {label: 'RTO/RPO target', detail: 'Commit to specific numbers, for example RTO of five minutes for reads and fifteen minutes for writes, RPO under sixty seconds for the primary dataset, derived from measured replication lag and failover automation time rather than a marketing SLA.'},
      {label: 'Failover mechanics', detail: 'Automate the failover decision for read traffic on multi-signal health checks; gate write failover behind a promotion runbook that checks replication lag and fences the old primary before opening the new one, so two regions never accept writes at once.'},
      {label: 'Validation', detail: 'Run scheduled regional failover drills against real or shadowed production traffic, measure the actual detected RTO/RPO, and treat every drill that misses target as an incident requiring a fix rather than a rescheduled drill.'},
    ],
    tradeoffs: [
      {decision: 'Active-active vs active-passive for writes', recommendation: 'Single-writer active-passive per shard/tenant with active-active reads', alternative: 'Multi-writer active-active only when the domain has well-tested conflict resolution and the business explicitly accepts eventual write consistency'},
      {decision: 'Anycast/global load balancer vs DNS-based failover', recommendation: 'Anycast or a global load balancer where the provider supports it, for faster and more reliable failover than DNS alone', alternative: 'DNS-based failover when the provider lacks anycast and the client population is known to respect low TTLs'},
      {decision: 'Synchronous vs asynchronous cross-region replication', recommendation: 'Asynchronous replication with a monitored lag budget for most datasets', alternative: 'Synchronous or quorum writes only for the small subset of data where losing seconds of writes is unacceptable and the added latency is affordable'},
      {decision: 'Automatic vs manual write failover', recommendation: 'Automatic failover for read traffic; a fast, scripted, human-triggered runbook for write promotion', alternative: 'Fully automatic write failover only once fencing and split-brain prevention have been proven correct under repeated drills'},
    ],
    failureModes: [
      {failure: 'DNS failover is triggered but a large share of clients keep resolving the dead region for twenty or more minutes', response: 'Detect via edge/CDN health checks and traffic-shape monitoring rather than trusting DNS propagation; where possible use anycast or a global load balancer that does not depend on client cache expiry; keep the dead region rejecting fast instead of hanging, and communicate degraded status.'},
      {failure: 'The write region fails during promotion and the old primary comes back online mid-failover', response: 'Fence the old primary immediately, revoking its ability to accept writes, before the new region opens for writes; detect via replication and connectivity heartbeats; if fencing cannot be confirmed, hold writes and serve reads only until the dual-writer risk is eliminated.'},
      {failure: 'Replication lag silently exceeds the committed RPO before an outage occurs', response: 'Alert on lag against the committed RPO well before failover is ever needed, not only during an incident; if a failover is forced anyway, treat the resulting gap as measurable data loss, reconcile from logs or queues after recovery, and revise the RPO commitment based on the evidence.'},
      {failure: 'Failback to the original region reintroduces stale or divergent data', response: 'Treat failback as a second failover: resync from the current authoritative region, validate reconciliation before flipping write ownership back, and run the same drill discipline used for the original failover.'},
    ],
    successMetrics: ['Measured RTO and RPO from real drills, not documented targets', 'Share of client traffic that actually fails over within the target window', 'Replication-lag distribution against the committed RPO budget', 'Failed-drill rate and time to close the resulting action items'],
    followUps: ['A partial regional outage degrades only the database tier while compute stays healthy — does your failover trigger correctly?', 'One enterprise customer contractually requires their data never leave a specific region — how does that change the design?', 'The secondary region is at sixty percent capacity when the primary fails and traffic doubles overnight — what happens?'],
    strongSignals: ['Names a specific RTO/RPO number and derives it from measured components, not aspiration', 'Treats DNS TTL as a floor and designs around clients that ignore it', 'Classifies data by staleness tolerance instead of one blanket consistency model', 'Designs fencing to prevent dual writers during promotion', 'Insists on regular, tested failover and failback rather than a documented runbook alone'],
  },
  {
    id: 'serverless-batch-pipeline',
    title: 'Serverless-first cost-optimized batch data pipeline',
    category: 'Migration & economics', difficulty: 'Senior', duration: 45,
    brief: 'A retail analytics team runs a daily event-driven batch pipeline that ingests clickstream and transaction data. Volume is bursty, ten times higher on a promotional Saturday than a normal Tuesday, and the current always-on cluster is sized for the peak, sitting mostly idle the rest of the time. Finance wants a real cost reduction without missing the nightly SLA that feeds executive dashboards.',
    ask: 'Design the ingestion and processing path and the cost model behind it. Explain how the pipeline absorbs bursts without falling behind, and exactly where spot or preemptible compute is safe to use.',
    requirements: ['Nightly SLA: dashboards refreshed within four hours of the last event', 'Cost scales with actual volume processed rather than peak provisioned capacity', 'No data loss when downstream processing falls behind ingestion', 'Auditable, safe reprocessing if a stage fails partway through'],
    unknowns: ['Exact burst shape and peak-to-trough ratio across the year', 'Whether every stage is idempotent and safely replayable', 'Downstream consumer tolerance for partial or delayed data', 'Current spend breakdown between always-on baseline and burst capacity'],
    constraints: ['Existing pipeline runs on a fixed-size cluster sized for the worst day', 'Some transformation steps are stateful and order-sensitive', 'No dedicated engineer is available for twenty-four-hour babysitting', 'Finance wants a quarter-over-quarter cost reduction target, not only a design'],
    answerOutline: [
      {label: 'Ingestion buffering', detail: 'Place a durable, horizontally scalable queue or stream between event producers and processing, sized for burst ingestion rate rather than processing rate; this queue is the backpressure boundary that keeps ingestion lossless even when downstream falls behind.'},
      {label: 'Elastic processing', detail: 'Run auto-scaled or serverless batch compute that scales workers to queue depth instead of a fixed cluster, so cost tracks actual volume processed and idle time between runs approaches zero.'},
      {label: 'Backpressure handling', detail: 'When consumers cannot keep up, let the queue absorb the backlog up to a bounded retention window instead of dropping or blocking producers; expose oldest-message age as the primary lag signal and alert on it approaching the SLA budget, not on raw queue depth.'},
      {label: 'Spot/preemptible economics', detail: 'Run the bulk of stateless, idempotent transformation stages on spot or preemptible capacity, since a preempted worker simply requeues its partition; keep stateful or order-sensitive stages on on-demand capacity where a mid-task preemption is expensive to recover from.'},
      {label: 'Idempotency and replay', detail: 'Design every stage to be safely re-runnable from its input partition using dedupe keys and idempotent writes, so a spot preemption or partial failure produces a retry rather than corruption or manual cleanup.'},
      {label: 'Cost governance', detail: 'Track cost per gigabyte processed and per pipeline run, not only total monthly spend, so the team can distinguish genuine efficiency gains from a scaling floor that quietly threatens the SLA.'},
    ],
    tradeoffs: [
      {decision: 'Fixed always-on cluster vs elastic, scale-to-zero processing', recommendation: 'Elastic, queue-depth-driven scaling for this bursty workload', alternative: 'A small always-on baseline only if scale-from-zero cold-start latency would itself threaten the SLA'},
      {decision: 'Spot/preemptible vs on-demand compute', recommendation: 'Spot for stateless, idempotent, cheaply-checkpointed stages', alternative: 'On-demand for stateful or order-sensitive stages where recovering from a preemption is expensive'},
      {decision: 'Buffer vs drop under backpressure', recommendation: 'Buffer in a durable stream with bounded retention and let processing catch up', alternative: 'Drop or sample only for genuinely best-effort telemetry where the business has explicitly accepted loss'},
      {decision: 'One large nightly batch vs many small micro-batches', recommendation: 'Micro-batches sized so individual retries stay cheap and parallelizable', alternative: 'One large batch only when the data has hard ordering dependencies that cannot be decomposed'},
    ],
    failureModes: [
      {failure: 'A promotional Saturday produces ten times normal volume and the queue backlog grows all day', response: 'Detect via oldest-message age crossing an SLA-derived threshold well before the four-hour deadline; scale processing workers further and, if capacity-constrained, shed load from low-priority event types first; after the backlog clears, reconcile counts against the source to confirm nothing was silently dropped.'},
      {failure: 'A wave of spot preemptions removes half the worker fleet mid-run', response: 'Detect via a drop in active workers and a spike in requeued partitions; the remaining workers and autoscaler absorb the requeued work because every stage is idempotent; if spot capacity in the region is genuinely exhausted, fall back to a smaller on-demand pool rather than stalling the whole run.'},
      {failure: 'A schema change in an upstream event silently corrupts the output of one stage', response: 'Validate schema at ingestion and quarantine non-conforming events into a dead-letter path instead of failing or silently passing them through; alert on dead-letter volume; replay the quarantined batch after a fix, using the same idempotent write path.'},
      {failure: 'The cost reduction target is hit but the nightly SLA starts slipping', response: 'Track cost per run and SLA attainment as paired metrics from the start; if the trade happened silently, treat the SLA miss as the primary signal and roll back the scaling floor rather than treating the cost win in isolation.'},
    ],
    successMetrics: ['SLA attainment: share of nights dashboards refresh within four hours', 'Cost per gigabyte processed and per pipeline run', 'Oldest-message age distribution during peak days', 'Spot preemption rate and requeue/retry success rate'],
    followUps: ['A downstream consumer now needs a subset of events within thirty minutes instead of four hours.', 'Spot capacity for the required instance family disappears in the region for a week.', 'The pipeline must now also support ad-hoc reprocessing of the last ninety days on demand.'],
    strongSignals: ['Separates the backpressure boundary from the compute scaling boundary', 'Matches spot usage to idempotency and checkpoint cost, not just to lowest price', 'Defines a concrete backlog-age alert threshold tied to the SLA', 'Tracks cost and SLA as paired metrics rather than optimizing cost in isolation'],
  },
  {
    id: 'progressive-delivery-at-scale',
    title: 'Zero-downtime progressive delivery across 200+ microservices',
    category: 'CI/CD & platform engineering', difficulty: 'Staff', duration: 60,
    brief: 'A platform team owns release tooling for a mesh of over two hundred independently deployed microservices across dozens of teams. Today most teams hand-roll their own deploy scripts, some still cut over manually, and a bad release in one service recently cascaded because no consistent automated rollback existed. Leadership wants a release strategy that scales without a human approving every deploy.',
    ask: 'Design the progressive-delivery platform: the canary vs blue/green choice at this scale, automated rollback triggers, and multi-region traffic shifting during a rollout.',
    requirements: ['Every service can release without a manual approval gate for low-risk changes', 'Automated rollback fires within minutes of a regression, not after a human notices', 'The rollout mechanism works consistently across services with different traffic and state shapes', 'Multi-region rollouts do not require duplicating the release process per region'],
    unknowns: ['Which services are stateless versus stateful, and which cannot support instant rollback', 'What SLIs already exist per service, and whether they are consistent enough to gate on', 'Traffic distribution across regions and whether a region can be drained without user impact', 'Current release frequency and blast-radius history per team'],
    constraints: ['Two hundred plus services owned by dozens of independent teams', 'No centralized on-call function can approve every release', 'Some services hold long-lived connections that complicate an abrupt cutover', 'A regulatory or contractual requirement to retain an audit trail of every production change'],
    answerOutline: [
      {label: 'Default strategy', detail: 'Standardize on canary as the default for the large majority of stateless services, since it shifts a small share of real traffic and can be evaluated against live SLIs without an all-or-nothing cutover; reserve blue/green for services with expensive warm-up or an incompatible in-place schema window that needs a fully-ready environment before any real traffic arrives.'},
      {label: 'Canary mechanics', detail: 'Shift traffic in small progressive steps, comparing the canary cohort against a control cohort on the same SLIs, and hold each step long enough to catch delayed failures rather than judging only on latency a minute after the step begins.'},
      {label: 'Automated rollback trigger', detail: 'Define the trigger as a statistically meaningful regression against a live baseline rather than a fixed absolute threshold, so it works across services with very different normal error rates; require rollback to fire and complete within a small number of minutes, and make rollback the tested default path, not an escalation.'},
      {label: 'Removing the human gate', detail: 'Replace manual approval with policy: automated promotion for changes that pass canary analysis within a pre-declared risk tier, and reserve mandatory human review for a narrow, explicitly declared high-risk tier such as schema migrations or authentication changes.'},
      {label: 'Multi-region rollout', detail: 'Roll out region by region rather than globally at once, treating the first region as an additional canary stage; shift regional traffic weights gradually and keep spare capacity in unaffected regions to absorb traffic if one region needs a full rollback.'},
      {label: 'Standardization without central review', detail: 'Give every team the same self-service pipeline template with canary analysis and rollback logic built in, so consistency comes from the shared platform rather than from a central team reviewing every release.'},
    ],
    tradeoffs: [
      {decision: 'Canary vs blue/green at this scale', recommendation: 'Canary by default for stateless services, since it validates against live traffic gradually and avoids running two full environments per service', alternative: 'Blue/green for services with expensive warm-up, incompatible in-place schema changes, or a hard requirement for instant full-traffic rollback'},
      {decision: 'Fixed-threshold vs baseline-relative rollback trigger', recommendation: 'A statistical comparison against a live control cohort', alternative: 'Fixed absolute thresholds only for a small set of safety-critical services with a well-understood, stable normal range'},
      {decision: 'Global rollout vs region-by-region', recommendation: 'Region-by-region rollout with the first region acting as an extra canary stage', alternative: 'Global simultaneous rollout only for low-risk changes on services with no meaningful regional traffic or data variance'},
      {decision: 'Automated promotion vs human approval', recommendation: 'Automated promotion gated on canary analysis for the declared low-risk tier', alternative: 'Mandatory human review for a narrow high-risk tier such as schema or authentication changes'},
    ],
    failureModes: [
      {failure: 'Canary analysis passes but a regression only appears under a traffic pattern absent during the canary window', response: 'Detect via post-rollout monitoring that keeps comparing full traffic against the pre-rollout baseline for a bounded window after full promotion; trigger the same automated rollback path used during canary if a delayed regression appears, and extend canary duration for services with known time-of-day traffic variance.'},
      {failure: 'Automated rollback fires but the service cannot roll back cleanly because of an in-place schema change', response: 'Require expand-contract schema migrations so the previous code version stays compatible during rollback; classify any release that breaks this rule as high-risk and route it to manual review instead of automated promotion.'},
      {failure: 'A region-by-region rollout leaves one region running two versions during a partial failure', response: 'Treat mixed-version state as expected and time-bounded rather than an incident by itself; alert only if the mixed-version window exceeds the expected rollout duration, and if a region cannot converge, roll that region back independently rather than blocking the others.'},
      {failure: 'A team gains automated promotion for a service whose SLIs are too noisy for canary analysis to be meaningful', response: 'Require a minimum SLI quality bar, a stable baseline and adequate traffic volume, before a service becomes eligible for automated promotion; services that fail the bar default to a longer canary hold or manual review until observability improves.'},
    ],
    successMetrics: ['Share of releases promoted automatically without manual intervention', 'Median and p95 time from regression onset to completed rollback', 'Change failure rate per team, and its trend over time', 'Rollout duration per region and total time to full global promotion'],
    followUps: ['One team owns a service with no meaningful SLIs at all — how does it participate in automated promotion?', 'A regulator now requires a human sign-off on every production change for one business line.', 'Two services must always deploy in lockstep because of a tight API contract — how does that interact with independent canary timing?'],
    strongSignals: ['Chooses canary vs blue/green based on workload shape, not a blanket rule', 'Defines the rollback trigger as baseline-relative rather than an arbitrary fixed number', 'Treats expand-contract schema migration as a prerequisite for safe automated rollback', 'Designs region-by-region rollout as a first-class part of the pipeline', 'Replaces manual approval with policy and risk tiers rather than removing safety altogether'],
  },
  {
    id: 'fintech-gitops-compliance',
    title: 'Regulated fintech GitOps CI/CD platform',
    category: 'CI/CD & platform engineering', difficulty: 'Senior', duration: 45,
    brief: 'A fintech company processing regulated payment data needs a GitOps-based deployment pipeline for its core ledger and payments services. A financial regulator\'s upcoming audit will examine how changes reach production, how secrets are handled, and whether every production change can be traced to an approved, evidenced decision. Today, some credentials live in plaintext CI variables and production deploys are sometimes triggered manually from a personal laptop.',
    ask: 'Design the pipeline\'s secrets management, audit-evidence generation, and change-approval trail so it satisfies the regulator without turning every release into a manual audit exercise. Zero GPU or ML content is in scope.',
    requirements: ['No plaintext secret is ever committed to or stored in Git', 'Every production change is traceable to an approved request, a reviewed diff and passing evidence', 'Audit evidence is generated automatically as a pipeline byproduct, not reconstructed after the fact from logs', 'A change-approval trail exists distinct from, and in addition to, normal code review'],
    unknowns: ['The exact regulatory control set and what an auditor actually samples during review', 'Current secret sprawl across CI variables, config files and shared drives', 'Whether change approval must be a named accountable individual or can be a role/policy', 'The retention period the regulator requires for audit evidence'],
    constraints: ['Ledger and payments services cannot tolerate scheduled deployment downtime', 'The existing CI system stores some secrets in plaintext variables today', 'A small platform team cannot manually assemble an audit package per release', 'The regulator requires segregation of duties between who writes code and who approves production change'],
    answerOutline: [
      {label: 'Secrets out of Git entirely', detail: 'Store no live credential in Git in any form, including an encrypted blob committed to the repository; commit only a reference to an external secrets manager, and have the deployment controller resolve and inject the real secret at reconciliation time, scoped to the workload identity that needs it.'},
      {label: 'Short-lived credential injection', detail: 'Issue short-lived, workload-scoped credentials at deploy or runtime rather than baking long-lived static secrets into images or config; rotate on a schedule and on every personnel change, and make rotation itself an evidenced, logged event.'},
      {label: 'Change-approval trail', detail: 'Require every production change to originate from a reviewed pull request, then layer a separate, explicit approval record on top that captures who requested the change, who approved it, what evidence they reviewed, and confirmation that the approver is not the author, distinct from routine code review.'},
      {label: 'Audit evidence as a pipeline byproduct', detail: 'Generate the audit record automatically at each stage, commit, review approval, security scan result, change approval, deployment reconciliation confirmation, and store it immutably and queryably, so producing an evidence package for a regulator request is a report against existing records, not a reconstruction project.'},
      {label: 'Segregation of duties', detail: 'Enforce with a policy check that the identity approving a production change is never the identity that authored or merged the code, rather than trusting process alone, and make bypass attempts themselves a logged, alerted event.'},
      {label: 'Continuous compliance', detail: 'Run the same control checks continuously against live cluster state, not only at deploy time, so drift introduced outside the pipeline, such as a manual edit during an incident, is detected and reconciled and appears in the same evidence trail.'},
    ],
    tradeoffs: [
      {decision: 'External secrets manager with references in Git vs encrypted secrets committed to Git', recommendation: 'An external secrets manager with only references committed to Git', alternative: 'Sealed or encrypted secrets committed to Git only as a transitional step with a firm migration deadline, since a key compromise still risks exposing the repository history'},
      {decision: 'Manual production approval vs automated policy-gated promotion', recommendation: 'A named human approval step for the regulated services in audit scope, satisfying segregation of duties explicitly', alternative: 'Automated policy-gated promotion for lower-risk services genuinely outside the audit scope'},
      {decision: 'On-demand vs continuous audit-evidence generation', recommendation: 'Continuous generation and immutable storage of evidence as each pipeline stage completes', alternative: 'On-demand reconstruction only for systems too small or short-lived to justify continuous evidence infrastructure'},
      {decision: 'Long-lived service credentials vs short-lived workload identity', recommendation: 'Short-lived, automatically rotated workload identity for all pipeline and runtime access', alternative: 'Long-lived credentials only for legacy systems that genuinely cannot integrate with a token exchange, paired with a compensating control and a remediation deadline'},
    ],
    failureModes: [
      {failure: 'An auditor asks for evidence that a production change from three months ago was approved by someone other than its author', response: 'Query the immutable audit store for the commit, review, approval and deployment records tied to that change; if the record is incomplete, treat the gap itself as a control failure, report it, and fix the pipeline stage that failed to emit evidence rather than reconstructing it from chat logs.'},
      {failure: 'A secret is accidentally committed to a feature branch in plaintext', response: 'Detect via a pre-commit or pipeline secret-scanning gate that blocks the push or fails the build immediately; if it reaches a shared branch, treat the credential as compromised regardless of whether it was merged, rotate it immediately, purge the branch history, and log the incident and the rotation as an evidenced event.'},
      {failure: 'The approver on a production change turns out to be the same person who authored the code, through a shared service account', response: 'Detect via an automated policy check comparing author and approver identity on every promotion, not a manual spot check; block promotion when the check fails; audit how the shared account was used and eliminate shared accounts as the systemic fix, not just the single incident.'},
      {failure: 'An engineer applies an emergency manual change directly to production during an incident, bypassing GitOps', response: 'Allow a time-bound, audited break-glass path rather than pretending it will never happen; require the emergency change to be captured as a reconciliation pull request within a fixed window afterward, and treat any emergency change that never gets reconciled back into Git as an open compliance finding.'},
    ],
    successMetrics: ['Share of production changes with a complete, automatically generated evidence trail', 'Mean time to produce an audit evidence package for a sampled change', 'Plaintext secrets found in Git history or CI variables: zero, continuously verified', 'Segregation-of-duties violations caught by policy versus caught manually after the fact'],
    followUps: ['The regulator requires evidence retention for seven years — how does that change your storage and key-rotation design?', 'A merger brings in a second CI system with a different approval model that must be reconciled before the next audit cycle.', 'An emergency break-glass change happens over a weekend when no one is available to approve reconciliation for four days.'],
    strongSignals: ['Removes plaintext secrets from Git and from CI variable stores as a hard requirement, not an aspiration', 'Designs a change-approval record explicitly separate from routine code review', 'Generates audit evidence continuously as a pipeline byproduct rather than reconstructing it after the fact', 'Enforces segregation of duties with policy checks, not trust in process', 'Designs a bounded, audited break-glass path instead of claiming the pipeline will never be bypassed'],
  },
  {
    id: 'hybrid-observability-federation',
    title: 'Hybrid cloud + on-prem observability federation',
    category: 'Reliability & operations', difficulty: 'Staff', duration: 60,
    brief: 'A company runs workloads across two public cloud providers and one on-premises data center, the result of a decade of acquisitions and regional expansion. Each environment has its own metrics and logging stack, and the current cross-environment view is a nightly batch job copying everything into one central time-series database, which is now falling behind and occasionally drops data during large ingestion spikes. Leadership wants one coherent operational view without recreating that fragile choke point.',
    ask: 'Design the observability federation across all three environments: how metrics, logs and traces are queried or centralized, how cardinality and network cost are controlled at this scale, and what happens when the federation layer itself has an outage.',
    requirements: ['Any on-call engineer can see cross-environment health during an incident without knowing which environment owns which service', 'Query latency for cross-environment dashboards stays interactive, not batch', 'Centralization never becomes a single point of failure for local alerting', 'Cost and cardinality growth stay bounded as environments and teams add signals'],
    unknowns: ['Current per-environment signal volume and cardinality growth rate', 'Network bandwidth and egress cost between the on-prem data center and each cloud', 'Whether local alerting already exists independently today or depends on the central system', 'Retention and data-residency requirements that differ by environment'],
    constraints: ['The on-prem data center has materially lower and less elastic network bandwidth than either cloud', 'The existing nightly batch copy job is the only current cross-environment view and is already failing under load', 'No team in any single environment has authority to dictate tooling standards to the other two', 'Historical acquisitions left genuinely different metric-label vocabularies across environments'],
    answerOutline: [
      {label: 'Local-first alerting', detail: 'Keep alert evaluation local to each environment against its own locally stored metrics, so a federation-layer outage never removes an environment\'s ability to detect and page on its own incidents; federation exists for cross-environment visibility and long-term analysis, not as a dependency of local paging.'},
      {label: 'Federated query over centralized copy', detail: 'Replace the nightly batch copy with a federated query layer that fans a cross-environment query out to each environment\'s local store at query time and merges results, instead of physically centralizing every raw sample; this bounds network cost to actual query traffic rather than continuous bulk replication.'},
      {label: 'Selective centralization', detail: 'Centralize only a deliberately small, downsampled set of cross-cutting SLIs and aggregates needed for executive dashboards, trend analysis and cross-environment correlation during incidents, and leave high-cardinality raw detail local and queryable on demand.'},
      {label: 'Cardinality and label normalization', detail: 'Define a small shared label contract, environment, service, region, owner, that every environment maps its local labels onto at the federation boundary, without forcing any environment to rewrite its internal taxonomy; quarantine series that cannot be mapped rather than letting them silently explode shared cardinality.'},
      {label: 'Network-cost-aware design', detail: 'Treat the on-prem-to-cloud link as the scarcest resource: prioritize downsampled aggregates and query results over raw sample replication, cache repeated cross-environment queries, and budget bandwidth explicitly for observability traffic so it cannot starve production traffic on the same link.'},
      {label: 'Federation-layer failure behavior', detail: 'When the federation layer itself is down, cross-environment dashboards degrade to unavailable or stale, but every environment keeps alerting and can be inspected locally; document and rehearse this degraded mode so on-call actually knows what to do, rather than discovering it during a real incident.'},
    ],
    tradeoffs: [
      {decision: 'Federated query-time fan-out vs continuous central replication', recommendation: 'Federated query at read time for most cross-environment views', alternative: 'Continuous replication only for the small set of aggregates that genuinely need low-latency centralized access, such as an executive-facing SLO dashboard'},
      {decision: 'Central alerting vs local-first alerting', recommendation: 'Local alert evaluation in every environment, independent of the federation layer', alternative: 'Central alerting only for cross-environment composite conditions no single environment can evaluate alone, with explicit acceptance that those alerts are unavailable during a federation outage'},
      {decision: 'Forcing one shared label taxonomy vs a mapping layer at the boundary', recommendation: 'A mapping layer at the federation boundary that translates local labels from each environment onto a shared contract', alternative: 'A single enforced taxonomy only if all three environments can realistically migrate their instrumentation, which acquisitions rarely make feasible on a useful timeline'},
      {decision: 'Raw high-cardinality replication vs downsampled aggregates', recommendation: 'Keep raw high-cardinality data local and replicate only downsampled aggregates centrally', alternative: 'Replicate more raw detail centrally only for a narrowly scoped, budgeted investigation with an explicit retention and cost limit'},
    ],
    failureModes: [
      {failure: 'The federation query layer goes down during a cross-environment incident', response: 'Detect via health checks and synthetic queries built into the federation layer itself; every environment keeps alerting and serving local dashboards unaffected, so on-call falls back to checking each environment directly using a documented runbook; restore the federation layer and validate that historical query results were not silently degraded before trusting it again.'},
      {failure: 'A misconfigured exporter in one environment floods the federation boundary with a new high-cardinality label', response: 'Detect via cardinality-growth alerts at the boundary mapping layer; quarantine the offending series at the boundary rather than letting it propagate into shared aggregates or inflate central storage cost; notify the owning team and require the label to be fixed or excluded before re-admission.'},
      {failure: 'The on-prem-to-cloud link saturates because every dashboard fires cross-environment queries at once during a large incident', response: 'Detect via link-utilization and query-queue-depth monitoring; rate-limit and cache cross-environment queries, prioritize alerting-relevant queries over exploratory dashboard traffic, and add a circuit breaker that falls back to cached or stale results rather than letting observability traffic starve production traffic on the same link.'},
      {failure: 'Two environments report the same service as healthy and unhealthy at the same time because of clock skew or inconsistent health definitions', response: 'Detect via cross-checking timestamps and health-check definitions during the federated query merge; normalize against a documented health contract, and surface disagreement explicitly as unknown rather than silently picking one side.'},
    ],
    successMetrics: ['Cross-environment query latency for on-call dashboards during an incident', 'Alert-evaluation availability per environment, independent of federation-layer uptime', 'Bandwidth consumed by observability traffic on the on-prem link versus budget', 'Cardinality growth rate at the federation boundary and quarantine rate'],
    followUps: ['One cloud provider changes its metrics export API with thirty days notice — how much of the federation layer breaks?', 'A new compliance requirement forbids any on-prem log data from leaving the data center, even downsampled.', 'The on-prem data center loses its network link to both clouds for six hours during a real incident — walk through what an on-call engineer actually sees.'],
    strongSignals: ['Keeps alert evaluation local so a federation outage cannot remove local paging capability', 'Chooses federated query over continuous bulk replication as the default', 'Treats network bandwidth to on-prem as an explicitly budgeted, scarce resource', 'Designs a boundary mapping layer instead of forcing one taxonomy across acquired environments', 'Has a rehearsed, documented degraded mode for when the federation layer itself fails'],
  },
  {
    id: 'org-incident-dr-program',
    title: 'Org-wide incident-response and disaster-recovery program',
    category: 'Reliability & operations', difficulty: 'Senior', duration: 45,
    brief: 'An engineering organization of forty independent teams, grown through acquisition and organic hiring, has ad hoc incident processes and inconsistent on-call rotations. One team runs GPU-backed ML training, but it is just one ordinary workload among many here. Disaster-recovery testing has historically meant one engineer manually failing over one system a year, if that. A recent multi-team incident took six hours partly because nobody could determine who owned escalation past the first responder. Leadership wants a consistent org-wide incident-response and disaster-recovery program, not another runbook template.',
    ask: 'Design the on-call and escalation structure across team boundaries, the role of chaos engineering and game days in validating readiness, and a recurring, budgeted disaster-recovery testing program across cloud regions.',
    requirements: ['Escalation past the first responder is unambiguous within minutes for any team, not only teams with mature on-call', 'Every team, regardless of workload type, participates in the same incident-severity and escalation framework', 'Disaster-recovery testing happens on a recurring schedule with a real budget, not as an annual best-effort exercise', 'Chaos-engineering and game-day findings translate into tracked fixes, not just anecdotes'],
    unknowns: ['Current on-call maturity and tooling variance across the forty teams', 'Which systems are genuinely cross-team dependencies whose failure requires multi-team escalation', 'Existing incident-severity definitions, if any, and whether they are consistent across teams', 'Appetite and budget for running chaos experiments and DR tests against production versus staging only'],
    constraints: ['Forty independent teams with different management chains and existing tooling', 'GPU training workloads exist but must be treated as one ordinary tenant, not a special case for this program', 'Leadership wants measurable progress within one or two quarters, not a multi-year overhaul', 'Some acquired teams carry contractual or regulatory incident-notification obligations that predate the org-wide program'],
    answerOutline: [
      {label: 'Common severity and escalation framework', detail: 'Define one org-wide incident-severity scale and escalation ladder that every team adopts as the floor, keeping their own runbooks underneath it; escalation past the first responder follows a timed, automatic path, unacknowledged after a defined number of minutes escalates to a named secondary and then to an incident-commander pool, rather than depending on someone remembering who to call.'},
      {label: 'Incident-commander rotation', detail: 'Stand up a cross-team incident-commander rotation, separate from any single team\'s on-call, whose job during a multi-team incident is coordination and communication rather than fixing the system directly; this role is exactly what was missing in the six-hour incident.'},
      {label: 'On-call baseline, not uniformity', detail: 'Require every team to meet a minimum on-call bar, a defined escalation policy, tested paging, a documented severity mapping, without forcing identical tooling; the org-wide layer integrates with whatever paging tool each team already uses instead of mandating one on day one.'},
      {label: 'Chaos engineering and game days as validation', detail: 'Run regular game days that inject realistic failures, including cross-team dependency failures, and score them against defined success criteria such as detection time, correct escalation and communication quality, producing a tracked list of gaps exactly like a production postmortem would; treat a skipped game day as a compliance gap, not a scheduling footnote.'},
      {label: 'DR testing as a recurring, budgeted program', detail: 'Establish a standing calendar of regional failover and restore tests across the estate, funded and staffed as an ongoing program with a named owner rather than a one-time exercise; each test has a pass/fail bar tied to a committed RTO/RPO, and a failed test blocks sign-off until fixed and re-tested.'},
      {label: 'Ordinary tenancy for specialized workloads', detail: 'Fold the GPU training team into the same severity, escalation and DR cadence as every other team; its workload-specific recovery mechanics, such as checkpoint restore time, are simply one more entry with its own RTO/RPO in the same program, not a parallel process.'},
    ],
    tradeoffs: [
      {decision: 'One mandated paging tool org-wide vs a common policy layer over existing tooling', recommendation: 'A common severity and escalation policy layer integrated with the tooling each team already uses', alternative: 'A single mandated tool once the policy layer is proven and migration cost is justified by consolidation savings'},
      {decision: 'Chaos experiments in production vs staging only', recommendation: 'Start with a bounded blast radius in production for well-understood failure classes, then expand', alternative: 'Staging-only indefinitely for systems where a failed experiment would itself cause a customer-visible incident and the organization is not yet ready for that risk'},
      {decision: 'Annual DR exercise vs a recurring budgeted program', recommendation: 'A recurring quarterly or more frequent program with a named owner and dedicated budget', alternative: 'A lower frequency only for systems with a genuinely low change rate and low business impact, documented explicitly rather than defaulted to by neglect'},
      {decision: 'Central incident-commander pool vs escalation staying within the originating team', recommendation: 'A cross-team incident-commander pool for anything crossing team boundaries or exceeding a severity threshold', alternative: 'Escalation staying within the originating team only for incidents confirmed single-team in scope from the start'},
    ],
    failureModes: [
      {failure: 'A multi-team incident recurs and again takes hours because the incident-commander rotation was never actually staffed', response: 'Detect via a paging test showing no one on the rotation acknowledges within the defined window; escalate to a named executive sponsor since an unstaffed rotation is a program failure, not an individual incident; require the gap to be fixed and re-validated with a drill before considering the program in place.'},
      {failure: 'A game day injects a failure that spills outside its intended blast radius and causes real customer impact', response: 'Detect via the same monitoring that would catch a real incident; treat it as a real incident with the standard severity and escalation process, since the customer impact is real regardless of cause; afterward, tighten blast-radius controls and the approval process for future game days at that scope.'},
      {failure: 'A regional DR test reveals the committed RTO is unachievable because a dependency outside the tested system was never included in the failover', response: 'Detect via the test itself failing against its pass/fail bar; do not sign off the test as passed with a caveat; expand the tested boundary to include the missed dependency, fix it, and re-run the full test before the RTO commitment is considered valid.'},
      {failure: 'An acquired team with a regulatory notification obligation is left out of the org-wide severity mapping and misses a mandated external notification during an incident', response: 'Detect via post-incident review cross-checking notification obligations against the incident timeline; treat the miss as a program-design gap, map every team\'s regulatory notification obligations into the org-wide severity and escalation framework, and verify the fix in the next game day.'},
    ],
    successMetrics: ['Time from incident detection to correct cross-team escalation, measured against target', 'Share of teams meeting the on-call baseline bar', 'Game days run per quarter versus planned, and share of findings closed within a defined window', 'Measured DR test RTO/RPO against committed targets, and pass rate over time'],
    followUps: ['Two teams disagree on who owns escalation for a shared dependency neither fully controls — how does the framework resolve that before the next incident, not during it?', 'Budget for the DR testing program is cut in half next year — what do you protect first?', 'A newly acquired team resists adopting the common severity framework because its regulator already mandates a different one — how do you reconcile it?'],
    strongSignals: ['Designs a timed, automatic escalation path rather than relying on someone knowing who to call', 'Separates a cross-team incident-commander role from any single team\'s on-call', 'Treats a skipped or failed game day/DR test as a program failure requiring escalation, not a scheduling footnote', 'Sets a policy floor across teams without forcing identical tooling on day one', 'Keeps GPU or other specialized workloads as ordinary tenants in the same program rather than special-casing them'],
  },
];
